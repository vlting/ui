import { createStub } from '../_stub'

export type TooltipProps = Record<string, any>
export type TooltipProviderProps = Record<string, any>

export const Tooltip = createStub('Tooltip', 'div')
export const TooltipProvider = createStub('TooltipProvider', 'div')

import { createStub } from '../_stub'

export type ComboboxOption = Record<string, any>
export type ComboboxRootProps = Record<string, any>
export type ComboboxItemProps = Record<string, any>

export const Combobox = {
  Root: createStub('Combobox.Root', 'div'),
}

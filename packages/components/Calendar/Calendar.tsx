import { createStub } from '../_stub'

export type CalendarRootProps = Record<string, any>
export type CalendarDayProps = Record<string, any>

export const Calendar = {
  Root: createStub('Calendar.Root', 'div'),
}

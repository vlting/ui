'use client'

interface PlaygroundSectionProps {
  slug: string
}

export function PlaygroundSection({ slug }: PlaygroundSectionProps) {
  // Playground disabled until stl Vanilla Extract integration is configured
  return null
}

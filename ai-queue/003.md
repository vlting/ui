<!-- auto-queue -->
# Task: Restructure packages — remove `src` nesting and uppercase component folders

## Objective
Flatten the `packages/*/src/` directory structure by removing the `src` level, and rename all component folders to start with an uppercase letter (matching their component file names).

## Before → After Examples

```
packages/components/src/button/Button.tsx  →  packages/components/Button/Button.tsx
packages/components/src/card/Card.tsx      →  packages/components/Card/Card.tsx
packages/components/src/dialog/Dialog.tsx  →  packages/components/Dialog/Dialog.tsx
packages/components/src/input/Input.tsx    →  packages/components/Input/Input.tsx
packages/components/src/tabs/Tabs.tsx      →  packages/components/Tabs/Tabs.tsx
packages/components/src/index.ts           →  packages/components/index.ts

packages/primitives/src/Box.tsx            →  packages/primitives/Box.tsx
packages/primitives/src/index.ts           →  packages/primitives/index.ts

packages/headless/src/checkbox/            →  packages/headless/Checkbox/
packages/headless/src/dialog/              →  packages/headless/Dialog/
packages/headless/src/tabs/                →  packages/headless/Tabs/
packages/headless/src/index.ts             →  packages/headless/index.ts

packages/hooks/src/index.ts                →  packages/hooks/index.ts
packages/hooks/src/useControllableState.ts →  packages/hooks/useControllableState.ts

packages/design-tokens/src/base.ts         →  packages/design-tokens/base.ts
packages/design-tokens/src/brands/         →  packages/design-tokens/brands/
packages/design-tokens/src/index.ts        →  packages/design-tokens/index.ts

packages/utils/src/cn.ts                   →  packages/utils/cn.ts
packages/utils/src/index.ts                →  packages/utils/index.ts
```

## Steps

### 1. Move files out of `src/` for each package
For each package in `packages/`:
```bash
# For each packages/<pkg>/src/ directory:
# Move all contents up one level, then remove the empty src/ dir
```

### 2. Rename lowercase component folders to uppercase
Only applies to folders that contain a component file (not utility/config folders):
- `packages/components/button` → `packages/components/Button`
- `packages/components/card` → `packages/components/Card`
- `packages/components/dialog` → `packages/components/Dialog`
- `packages/components/input` → `packages/components/Input`
- `packages/components/tabs` → `packages/components/Tabs`
- `packages/headless/checkbox` → `packages/headless/Checkbox`
- `packages/headless/dialog` → `packages/headless/Dialog`
- `packages/headless/tabs` → `packages/headless/Tabs`

Do NOT rename:
- `packages/design-tokens/brands/` — this is a config folder, not a component folder
- Any folder that doesn't directly correspond to a component name

### 3. Update ALL internal import paths
This is the most critical step. Every import that referenced `packages/*/src/...` must be updated. Search for all occurrences across the entire repo:

- `src/index.ts` — re-exports from `../packages/*/src` → `../packages/*`
- `config/tamagui.config.ts` — imports from `../packages/design-tokens/src/brands` → `../packages/design-tokens/brands`
- `packages/*/index.ts` — internal imports (e.g., `./button/Button` → `./Button/Button`)
- `packages/design-tokens/brands/index.ts` — relative imports like `../base` should still work after flattening
- Root `package.json` exports map — paths like `"./design-tokens": { "import": "./packages/design-tokens/src/index.ts" }` → `"./design-tokens": { "import": "./packages/design-tokens/index.ts" }`
- Example apps — any imports that reach into `packages/*/src/`
- Test files — any imports from package internals
- `tsconfig.json` / `tsconfig.*.json` — path mappings if any reference `src/`

### 4. Use `git mv` for all moves
Use `git mv` instead of plain `mv` to preserve git history tracking:
```bash
git mv packages/components/src/button packages/components/Button
```
Note: On case-insensitive filesystems (macOS default), renaming just the case may need a two-step move (e.g., `git mv button tmp && git mv tmp Button`).

### 5. Verify nothing is broken
After all moves and import updates:
- `yarn typecheck` should pass (or at least not have new errors)
- `yarn dev:kitchen-sink` should still work
- `yarn dev:crushd` and `yarn dev:volta` should still work (if they exist)
- All `index.ts` barrel files should still export correctly

### 6. Use grep to find any remaining stale references
```bash
grep -r "packages/.*src/" --include="*.ts" --include="*.tsx" --include="*.json" .
```
Fix any remaining references found.

## Important Notes
- **Do NOT modify any component code** — this is purely a file/folder restructuring task
- **Preserve all git history** by using `git mv`
- **On macOS**: The filesystem is case-insensitive by default, so `git mv button Button` may fail. Use a temp name: `git mv button _Button && git mv _Button Button`
- Be thorough with import path updates — a missed one will cause a build failure

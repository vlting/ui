<!-- auto-queue -->
<!-- depends-on: 003, 004, 005, 006, 007 -->
# Integration: update barrel exports, clean up headless, update kitchen-sink

## Context

This is the integration segment for the Tamagui component migration (tasks 003-007). It handles shared files that multiple previous tasks affect: barrel exports, headless package cleanup, and kitchen-sink example updates.

## Implementation

### 1. Update barrel exports (`packages/components/index.ts`)

Add exports for new components created in task 007:
```tsx
export { Popover } from './Popover/Popover'
export { Sheet } from './Sheet/Sheet'
export { Toast, ToastProvider, ToastViewport } from './Toast/Toast'
export { Menu } from './Menu/Menu'
```

Verify all existing exports still work (component names haven't changed, only implementations).

### 2. Clean up headless package

After tasks 003-006, our custom headless components become unused because Tamagui's official components provide the behavior built-in.

**Evaluate for removal or deprecation:**
- `packages/headless/Dialog/Dialog.tsx` — replaced by `@tamagui/dialog` (task 005)
- `packages/headless/Tabs/Tabs.tsx` — replaced by `@tamagui/tabs` (task 006)
- `packages/headless/Checkbox/Checkbox.tsx` — replaced by `@tamagui/checkbox` (task 004)

**Options:**
- **Option A (recommended)**: Keep the headless files but add deprecation comments at the top of each file: `/** @deprecated Use the styled component which now uses @tamagui/{name} internally */`. Update `packages/headless/index.ts` to note deprecation. This maintains backwards compatibility for any external consumers.
- **Option B**: Remove the headless files entirely if no external consumers depend on them. Check for imports from `packages/headless/` in the codebase first.

**Also evaluate hooks:**
- `packages/hooks/useControllableState.ts` — still useful as a general-purpose hook, KEEP.
- `packages/hooks/useFocusTrap.ts` — replaced by `@tamagui/focus-scope`. Deprecate or remove.
- `packages/hooks/useKeyboardNavigation.ts` — replaced by `@tamagui/roving-focus`. Deprecate or remove.

### 3. Update kitchen-sink example

The kitchen-sink example app uses our components. After tasks 003-007, the component APIs should be backwards-compatible, but verify:

1. **Build check**: Run the kitchen-sink build to ensure no import/type errors.
2. **Visual check**: Ensure components render correctly with brand theming.
3. **Add showcase pages** for new components (Popover, Sheet, Toast, Menu) — create simple demo pages.
4. **Update existing pages** if any component APIs have changed (e.g., new sub-components added).
5. **Example pages that use raw `<button>` / `<input>` with inline styles**: Consider replacing with our newly-improved components (e.g., `ComposedPage.tsx`, `HeadlessPage.tsx`, `HooksPage.tsx` use raw `<button>` with `style={{...reset}}>` wrapping `<View>` — these should use our `<Button>` component instead).

### 4. Update any remaining example apps

Check `examples/dating/` and `examples/volta/` for:
- Raw `<input>` / `<textarea>` with inline styles → replace with `<Input>` / `<Textarea>` components
- Raw `<button>` with inline styles → replace with `<Button>` component
- Any other patterns that should use the improved components

### 5. Spec files

Check if any `.spec.md` files reference the old headless approach or old component internals. Update them to reflect the new Tamagui-based implementation.

## Verification

- Kitchen-sink builds without errors
- All component pages render correctly
- Brand switcher still works (components respond to theme changes)
- New component showcase pages are accessible
- No unused imports or dead code warnings

## Scope

- `packages/components/index.ts`
- `packages/headless/index.ts`
- `packages/headless/Dialog/Dialog.tsx`
- `packages/headless/Tabs/Tabs.tsx`
- `packages/headless/Checkbox/Checkbox.tsx`
- `packages/hooks/useFocusTrap.ts`
- `packages/hooks/useKeyboardNavigation.ts`
- `examples/kitchen-sink/src/**` (page updates + new showcase pages)
- `examples/dating/src/**` (raw input/button replacement)
- `examples/volta/src/**` (raw input/button replacement)

<\!-- LAT: 2026-03-10T04:19:45Z -->
<\!-- PID: 28592 -->
<!-- auto-queue -->
<!-- target-branch: feat/component-quality/interaction-states -->
# Task: Add interaction states to Checkbox, Switch, Select

## Goal
Add :hover, :focus-visible, :active, :disabled states using STL tokens.

## Instructions

1. Read `packages/COMPONENT_QUALITY_REPORT.md` for context

2. For each component:

### Checkbox (`packages/components/Checkbox/Checkbox.tsx`)
- Replace hardcoded `#0066ff`, `#d1d5db` with STL color tokens
- Add `:hover` — border/background color shift via token
- Add `:focus-visible` — outline ring using outline tokens
- Add `:active` — subtle press feedback
- Verify `:disabled` — reduced opacity
- Add smooth transitions

### Switch (`packages/components/Switch/Switch.tsx`)
- Replace hardcoded `rgba(0,0,0,0.2)`, `#fff` with STL tokens
- Add `:hover` — track color shift
- Add `:focus-visible` — outline ring on thumb or track
- Add `:active` — slight thumb expansion (common UX pattern)
- Verify `:disabled` — reduced opacity
- Add smooth transitions

### Select (`packages/components/Select/Select.tsx`)
- Replace hardcoded `rgba(0,0,0,0.15)`, `#f3f4f6` with STL tokens
- Add `:hover` — border/background shift
- Add `:focus-visible` — outline ring
- Verify `:disabled` — cursor not-allowed
- Add smooth transitions

## Pattern
Use STL token references (var(--stl-*)) consistently. Check the existing styled() pattern used in each component.

## Key files
- `packages/components/Checkbox/Checkbox.tsx`
- `packages/components/Switch/Switch.tsx`
- `packages/components/Select/Select.tsx`

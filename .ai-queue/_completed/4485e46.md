# Commit History

- `dd9d72a` — feat: rewrite to layered, domain-free architecture
- `4485e46` — feat: layered, domain-free architecture rewrite (merge)

---

# Task: Rewrite Design System — Layered, Domain-Free Architecture

## Completed

Fully replaced 24 domain-specific modules (238 stub components) with a clean, layered, domain-free design system.

### Deliverables

1. **Full folder structure** — 6 packages, 41 files, all with barrel exports
2. **Token definitions** — complete base + semantic color tokens in `packages/design-tokens/`
3. **Base theme implementation** — light/dark themes + 7 accent colors via theme-builder
4. **Example brand override** — "neon" brand (`packages/design-tokens/src/brands/neon.ts`) with cyan/teal palettes, rounded radius, uppercase headings
5. **5 fully implemented components**: Button, Dialog, Input, Tabs, Card
6. **Composability demo** — headless Dialog/Tabs + styled wrappers show layer composition
7. **Dual-brand ready** — `createBrandConfig(neonBrand)` produces a completely different visual identity

### Architecture

```
packages/
  design-tokens/   — tokens, themes, palettes, shadows, brand system
  primitives/      — Box, Stack, Text, Heading, Icon, Divider, Spacer, Portal
  headless/        — Dialog, Tabs, Checkbox (accessible, styling-agnostic)
  components/      — Button, Input, Card, Dialog, Tabs (styled, themed)
  hooks/           — useControllableState, useFocusTrap, useKeyboardNavigation
  utils/           — mergeRefs, composeEventHandlers, cn
```

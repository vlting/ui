<!-- LAT: 2026-03-17T04:14:32Z -->
<!-- PID: 67817 -->
<!-- auto-queue -->
<!-- target-branch: feat/library-buildout/feedback-components -->

# Task: Semantic HTML for Empty component

## Objective

Improve semantic HTML in `Empty` component — the only implemented component with meaningful element upgrades.

## Changes

**File: `packages/components/Empty/Empty.tsx`**

| Sub-component | Current | Target | Why |
|---------------|---------|--------|-----|
| `EmptyRoot` | `div` | `section` | Sectioning element; groups the empty-state content. `role="status"` stays (explicit role overrides implicit). |
| `EmptyMedia` | `div` | `figure` | Contains an illustration/icon — that's exactly what `<figure>` is for. |
| `EmptyAction` | `div` | `footer` | Concluding action area within a section — semantic match for `<footer>`. |

Leave `EmptyTitle` (`h3`) and `EmptyDescription` (`p`) unchanged — already correct.

**File: `packages/components/Empty/Empty.spec.md`**

Update the anatomy section to reflect the new HTML elements (`section`, `figure`, `footer`).

## Checklist

- [ ] Change `EmptyRoot` from `styled('div', …)` to `styled('section', …)`
- [ ] Change `EmptyMedia` from `styled('div', …)` to `styled('figure', …)`
- [ ] Change `EmptyAction` from `styled('div', …)` to `styled('footer', …)`
- [ ] Update `Empty.spec.md` anatomy to match new elements
- [ ] Run `npx jest packages/components/Empty` — tests pass
- [ ] Run `npx tsc --noEmit` — no new errors in Empty files
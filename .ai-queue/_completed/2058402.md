# Commit History
- 2058402 — Merge branch 'q/003-s4' into docs/component-parity/api-mapping
- 7cb8cb3 — docs(component-parity): add root api-mappings.json, migration guide, and primitive audit

---
<!-- auto-queue -->
<!-- depends-on: 001, 002 -->
<!-- target-branch: docs/component-parity/api-mapping -->

# Root Aggregation + Migration Guide + Primitive Audit

Integration segment: create the root api-mappings.json, write the migration guide, and perform the primitive API audit.

## Scope

- `api-mappings.json` (new — root level)
- `docs/migration-guide.md` (new)
- `docs/primitive-api-audit.md` (new)

## Context

- **Depends on:** Segments 001 and 002 (all api-mapping.json files must exist first)
- **Feature flag:** `component_parity`
- Read `.ai-epics/docs/component-parity/tech-spec.md` for the schema and requirements.

## Instructions

### 1. `api-mappings.json` (root level)

Aggregate ALL api-mapping.json files from `packages/primitives/*/api-mapping.json` and `packages/components/*/api-mapping.json` into a single root file.

Structure:
```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "version": "1.0.0",
  "generated": "2026-03-01",
  "primitives": {
    "AspectRatio": { ... contents of AspectRatio/api-mapping.json ... },
    "Badge": { ... },
    ...
  },
  "components": {
    "Accordion": { ... contents of Accordion/api-mapping.json ... },
    "Alert": { ... },
    ...
  }
}
```

Read each api-mapping.json file and embed its contents. This is a machine-readable aggregate for tooling and AI agents.

### 2. `docs/migration-guide.md`

Write a prose migration guide covering:

**Structure:**
```markdown
# Migrating from shadcn/ui to @vlting/ui

## Quick Start
- Install: `npm install @vlting/ui`
- Change imports: `@/components/ui/*` → `@vlting/ui`

## Import Changes
| shadcn | @vlting/ui |
|--------|-----------|
| `import { Button } from '@/components/ui/button'` | `import { Button } from '@vlting/ui'` |
| ... | ... |

## Prop Renames
| Component | shadcn prop | @vlting/ui prop | Notes |
|-----------|-------------|----------------|-------|
| Button | variant="destructive" | variant="filled" tone="danger" | Two-prop pattern |
| ... | ... | ... | ... |

## Behavioral Differences
### className → Style Props
shadcn uses Tailwind CSS classes. @vlting/ui uses Tamagui style props for cross-platform support.
```before: <Button className="bg-red-500">```
```after: <Button backgroundColor="$red10">```

### asChild Composition
shadcn uses Radix's `asChild` pattern. @vlting/ui uses direct composition.
{code examples}

### Cross-Platform Considerations
Components work on both web and React Native. Some features are web-only:
- DataTable (uses native HTML tables)
- CSS-based border-radius collapsing (InputGroup, ButtonGroup)
{...}

## Component-by-Component Guide
For each component with significant API differences, show before/after code snippets.

## New Components (not in shadcn)
- Direction (RTL/LTR context provider)
- Item (generic list item)
- InputGroup (input with addons)
- Toast imperative API (toast(), toast.success(), etc.)
- DataTable (wraps @tanstack/react-table + Table)
```

**Key principles:**
- Be concise but complete
- Focus on practical migration steps, not theory
- Include code examples for every breaking change
- Organize by frequency of impact (most common changes first)

### 3. `docs/primitive-api-audit.md`

Audit the 7 primitives mentioned in the acceptance criteria against their shadcn/Radix equivalents:

```markdown
# Primitive API Audit: shadcn Equivalence

## Summary
All 7 primitives (Badge, Kbd, Label, Separator, Skeleton, Spinner, AspectRatio) provide feature parity with their shadcn equivalents.

## Badge
| Feature | shadcn | @vlting/ui | Status |
|---------|--------|-----------|--------|
| variant prop | ✓ (default, secondary, destructive, outline) | ✓ (default, secondary, destructive, outline) | ✓ Parity |
| className | ✓ | ✗ (use Tamagui style props) | Breaking |
{...}

## Kbd
{...}

## Label
{...}

## Separator
{...}

## Skeleton
{...}

## Spinner
{...}

## AspectRatio
{...}
```

For each primitive:
1. Read the primitive's actual TypeScript types from `packages/primitives/{Name}/{Name}.tsx`
2. Compare against shadcn's equivalent (shadcn/ui uses Radix primitives for most of these)
3. Document parity, differences, and any missing features
4. Confirm that the API surface is equivalent for migration purposes

## Verification

- `api-mappings.json` is valid JSON and contains all components
- `docs/migration-guide.md` covers all major API differences
- `docs/primitive-api-audit.md` covers all 7 required primitives
- All files are well-formatted and readable

## Task Progress
<!-- lat: 2026-03-02T02:24:34Z -->
<!-- agent-pid: 36295 -->
<!-- worktree: .worktrees/q-003s4 -->
<!-- branch: q/003-s4 -->

### Checklist
- [ ] **ACTIVE** → Create root api-mappings.json
- [ ] Write migration guide
- [ ] Write primitive API audit
- [ ] Verify all files

### Handoff Context
- Depends on completed tasks 001 and 002 (all api-mapping.json files now in stage branch)
- Need to aggregate from packages/primitives/*/api-mapping.json and packages/components/*/api-mapping.json

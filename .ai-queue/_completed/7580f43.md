# Commit History
- `087d86a` fix(button): fix text contrast and implement tone variants
- `7580f43` fix(button): fix text contrast and implement tone variants (merge)

---

<!-- auto-queue -->
# Fix Button Contrast & Implement Tone Variants

## Result
- Fixed solid variant text contrast: uses `$color1` (lightest palette step) instead of `$color` (darkest), meeting WCAG AA 4.5:1 ratio
- Implemented tone variants via Tamagui `<Theme>` wrapper: primary→blue, success→green, warning→orange, danger→red
- ButtonContext passes variant to ButtonText for correct color resolution
- Removed empty tone no-op variants from ButtonFrame
- Kitchen-sink updated with 3x5 tone matrix (solid/outline/ghost × all 5 tones)
- Build verified clean, Vite production build succeeds

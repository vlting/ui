<!-- auto-queue -->
<!-- depends-on: 005 -->
# Commit History
- 9accb33 feat(examples): add right/left panel drawer examples to kitchen-sink
- 1a973dc feat(examples): overlay panel examples in kitchen-sink (#006)

# 006 — Kitchen-Sink: Overlay Examples (Drawers, Panels, HoverCard, Tooltips)

## Objective

Expand the kitchen-sink overlay examples to demonstrate:
- Bottom drawers (already exists, but update to reflect handle changes from task 005)
- Right panel (side drawer from the right)
- Left panel (side drawer from the left)
- HoverCard positioned correctly relative to its trigger
- Tooltips at corrected distance

## Context

- `OverlaysPage.tsx` currently only shows a single bottom drawer and basic HoverCard/Tooltip demos.
- The Drawer component supports `direction` prop: `'bottom' | 'top' | 'left' | 'right'`.
- After task 005, the Drawer handle logic will be conditional, HoverCard positioning will be fixed, and Tooltip offset will be corrected.
- This task adds comprehensive examples that demonstrate the updated behavior.

## Tasks

1. **Add right panel drawer example** to `examples/kitchen-sink/src/pages/OverlaysPage.tsx`:
   - New `DemoCard` with a "Right Panel" label.
   - Use `<Drawer.Root direction="right">` with content showing a settings/detail panel pattern.
   - No drag handle (desktop default from task 005).

2. **Add left panel drawer example**:
   - New `DemoCard` with a "Left Panel" label.
   - Use `<Drawer.Root direction="left">` with content showing a navigation/sidebar pattern.
   - No drag handle (desktop default).

3. **Update existing bottom drawer example**:
   - Keep the current bottom drawer.
   - Handle visibility should be automatic (hidden on desktop, shown on mobile after task 005).

4. **Enhance HoverCard examples**:
   - Show the hover card positioned correctly relative to its trigger (after task 005 fix).
   - If only one example exists, that's fine — just make sure it demonstrates proper positioning.

5. **Ensure Tooltip examples** reflect the corrected offset from task 005.

6. **Follow existing example page patterns**: Use the same `DemoCard`, `SectionTitle`, layout conventions as the rest of the page.

## Scope

- `examples/kitchen-sink/src/pages/OverlaysPage.tsx`

<!-- auto-queue -->
<!-- depends-on: 001, 002, 003 -->
<!-- target-branch: feat/token-audit-fonts/font-system-foundation -->

# Commit History
- `c774093` feat(token-audit-fonts): wire FontLoader into Provider and export new APIs
- `034fb35` feat(token-audit-fonts): merge segment 004 — Provider integration and exports

# Segment 4: Integration — Provider + exports

**Epic:** #12 | **Stage:** #13 (Font System Foundation)

## Scope
- `src/provider/Provider.tsx`
- `src/provider/index.ts`
- `packages/utils/index.ts`
- `packages/design-tokens/index.ts`
- `src/index.ts`

## Instructions

This is the integration segment that wires the FontLoader into the Provider and ensures all new exports are accessible from the package's public API. This segment depends on segments 001-003 being complete.

### 1. Update `src/provider/Provider.tsx`

Add the `FontLoader` component to the Provider and expose a `fontConfig` prop.

```typescript
import type { ReactNode } from 'react'
import { TamaguiProvider, type TamaguiProviderProps } from 'tamagui'
import type { BrandFontConfig } from '../../packages/design-tokens/brands'
import { FontLoader } from '../../packages/utils/FontLoader'
import defaultConfig from '../../config/tamagui.config'

export interface ProviderProps {
  children: ReactNode
  config?: TamaguiProviderProps['config']
  defaultTheme?: 'light' | 'dark'
  fontConfig?: BrandFontConfig
}

export function Provider({
  children,
  config = defaultConfig,
  defaultTheme = 'light',
  fontConfig,
}: ProviderProps) {
  return (
    <TamaguiProvider config={config} defaultTheme={defaultTheme}>
      <FontLoader fontConfig={fontConfig} />
      {children}
    </TamaguiProvider>
  )
}
```

**Key decisions:**
- `FontLoader` is rendered inside `TamaguiProvider` (before children) so it has access to the Tamagui context if needed in the future
- `fontConfig` is optional — omitting it means no Google Fonts loading (backward compatible)
- The `FontLoader` returns `null` so it doesn't affect the component tree

### 2. Update `src/provider/index.ts`

No changes needed — it already re-exports `Provider` and `ProviderProps`. The new `fontConfig` prop is part of `ProviderProps`.

### 3. Update `packages/utils/index.ts`

Add exports for FontLoader:

```typescript
export { mergeRefs } from './mergeRefs'
export { composeEventHandlers } from './composeEventHandlers'
export { cn } from './cn'
export { FontLoader, getGoogleFontsUrl } from './FontLoader'
export type { FontLoaderProps } from './FontLoader'
```

### 4. Update `packages/design-tokens/index.ts`

Add `BrandFontConfig` to the type exports:

Change the type export block from:
```typescript
export type {
  BrandDefinition,
  BorderConfig,
  OutlineConfig,
  AnimationConfig,
  TypographyConfig,
  TokenOverrides,
  FontOverrides,
} from './brands'
```

To:
```typescript
export type {
  BrandDefinition,
  BrandFontConfig,
  BorderConfig,
  OutlineConfig,
  AnimationConfig,
  TypographyConfig,
  TokenOverrides,
  FontOverrides,
} from './brands'
```

### 5. Update `src/index.ts`

Add the new exports to the main package index.

**In the design tokens type export block**, add `BrandFontConfig`:

```typescript
export type {
  BrandDefinition,
  BrandFontConfig,
  BorderConfig,
  OutlineConfig,
  AnimationConfig,
  TypographyConfig,
  TokenOverrides,
  FontOverrides,
  ShadowToken,
  ShadowScale,
} from '../packages/design-tokens'
```

**In the utils export block**, add `FontLoader` and `getGoogleFontsUrl`:

```typescript
// Utils
export {
  mergeRefs,
  composeEventHandlers,
  cn,
  FontLoader,
  getGoogleFontsUrl,
} from '../packages/utils'
export type { FontLoaderProps } from '../packages/utils'
```

## Verification

After all changes:

1. **TypeScript compilation**: Run `npx tsc --noEmit` from the repo root. Should pass with no errors.
2. **Import check**: Verify that a consumer could write:
   ```typescript
   import { Provider, FontLoader, getGoogleFontsUrl } from '@vlting/ui'
   import type { BrandFontConfig, FontLoaderProps, ProviderProps } from '@vlting/ui'
   ```
3. **Backward compatibility**: The `Provider` component still works without `fontConfig` — existing consumers won't break.
4. **Run existing tests** if available: `yarn test` or `npm test` — no regressions expected since this is purely additive.

## Task Progress
<!-- lat: 2026-03-01T21:32:44Z -->
<!-- agent-pid: 22715 -->
<!-- worktree: (pending) -->
<!-- branch: (pending) -->

### Checklist
- [ ] **ACTIVE** → Create worktree from target branch
- [ ] Update Provider.tsx with FontLoader and fontConfig prop
- [ ] Update packages/utils/index.ts with FontLoader exports
- [ ] Update packages/design-tokens/index.ts with BrandFontConfig export
- [ ] Update src/index.ts with new exports
- [ ] Verify TypeScript compiles
- [ ] Run tests if available

### Handoff Context
- Target branch: feat/token-audit-fonts/font-system-foundation
- Integration segment: depends on 001, 002, 003 all being merged

<!-- LAT: 2026-03-09T15:44:34Z -->
<!-- PID: 6276 -->
<!-- auto-queue -->
<!-- target-branch: feat/stl-components/compound-components -->
# Task: Migrate remaining components (Sidebar, Drawer, Resizable, InputOTP, Command, Calendar, DatePicker, Chart)

These are the remaining complex/behavioral components.

## Migration pattern
- Import `styled` from `'../stl-react/src/config'`
- Replace all Tamagui styled() and component imports with stl equivalents
- Keep behavioral logic (hooks, state, refs)

## Files to modify

### `packages/components/Sidebar/Sidebar.tsx`
- Replace Tamagui styled components with stl styled()
- Keep SidebarContext and all state management
- Keep compound pattern

### `packages/components/Drawer/Drawer.tsx`
- Replace Tamagui styled with stl styled()
- Keep drag handling logic (useState, useRef)
- CSS transitions for open/close

### `packages/components/Resizable/Resizable.tsx`
- Replace Tamagui styled with stl styled()
- Keep drag handling + panel sizing logic
- Keep ResizableContext

### `packages/components/InputOTP/InputOTP.tsx`
- Replace Tamagui styled with stl styled()
- Keep OTP input logic (focus management, paste handling)

### `packages/components/Command/Command.tsx`
- Replace Tamagui styled with stl styled()
- Keep command palette logic (search, filtering, keyboard nav)
- Use `useSearch` + `useListState` from stl-headless if applicable

### `packages/components/Calendar/Calendar.tsx`
- Replace Tamagui styled with stl styled()
- Keep date calculation logic
- Use `styled("table")`, `styled("td")` etc. for calendar grid

### `packages/components/DatePicker/DatePicker.tsx` and `DateRangePicker.tsx`
- Replace Tamagui styled + date components with stl styled()
- Keep date state management
- Use `useDisclosure` + `usePopoverPosition` for calendar popup

### `packages/components/Chart/Chart.tsx`, `ChartLegend.tsx`, `ChartTooltip.tsx`
- Replace Tamagui styled with stl styled()
- Keep Victory chart integration
- Keep tooltip positioning logic

## Acceptance criteria
- Zero Tamagui imports in all files
- All behavioral logic preserved
- All compound patterns preserved

<!-- auto-queue -->
# Commit History
- c8149a9 — fix(examples): replace broken tag= props with native HTML elements in kitchen-sink demos

<!-- depends-on: 001, 002, 003, 004, 005, 006, 007 -->
# Integration: Kitchen-Sink Demo Updates & Verification

## Priority: HIGH (run after all component a11y fixes)

This is the **integration segment**. It runs after tasks 001-007 which fix the underlying semantic HTML issues in the component library.

## Scope

Files modified:
- `examples/kitchen-sink/src/pages/ComponentsPage.tsx`
- `examples/kitchen-sink/src/pages/ComposedPage.tsx`
- `examples/kitchen-sink/src/pages/PrimitivesPage.tsx`

## Required Changes

### 1. Fix layout — stack form components vertically

In `ComponentsPage.tsx`, remove `<XStack gap="$3" flexWrap="wrap">` wrappers from form sections so DemoCards stack vertically at full width:

Sections to fix: Input, Textarea, Select, Checkbox, RadioGroup, Switch, Slider, Progress

Sections to keep horizontal: Button, Avatar, Toggle, Tooltip, Card

### 2. Update Button demos

Show variant x tone matrix (solid/outline/ghost across neutral/primary/success/warning/danger). Verify contrast is readable.

### 3. Update demos to match any API changes

Tasks 001-007 may have changed component APIs (e.g., new props, removed props, restructured compound components). Update demos to match.

### 4. Verify all interactive elements are keyboard-accessible

Navigate the entire kitchen-sink app using keyboard only. Every interactive element should:
- Be reachable via Tab
- Show a visible focus indicator
- Be activatable via Enter/Space
- Support arrow keys where appropriate (RadioGroup, Tabs, Slider)

### 5. Verify semantic HTML in browser devtools

Inspect the DOM and confirm:
- Buttons render as `<button>` (not `<div>` or `<span>`)
- Inputs render as `<input>` (not `<div>`)
- Textareas render as `<textarea>` (not `<span>`)
- Forms render as `<form>` (not `<div>`)
- Labels render as `<label>` (not `<span>`)
- Breadcrumb renders as `<nav>` with `<ol>`/`<li>`/`<a>`
- Card titles render as `<h3>`
- Table renders as `<table>` with proper table elements

### 6. Build verification

Run `yarn build` from repo root. Fix any TypeScript errors from changed APIs.

## Verification

- Build: `yarn build`
- Visual: `yarn dev:kitchen-sink` — all pages render correctly
- Keyboard: Full keyboard navigation works throughout
- DOM: Semantic HTML elements verified in devtools
- Light/dark mode, all 3 brands

## Constraints

- Read `AI_CONSTITUTION.md` before starting
- Adapt to API changes from tasks 001-007
- Do NOT modify component source files — only kitchen-sink demo files

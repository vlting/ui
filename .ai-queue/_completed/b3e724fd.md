<!-- LAT: 2026-03-12T03:16:25Z -->
<!-- PID: 23046 -->
<!-- worktree: .worktrees/q-003 -->
<!-- branch: q-003 -->
<!-- auto-queue -->
<!-- target-branch: chore/foundation-reset/design-tokens-migration -->
<!-- depends-on: 001 -->
# Task: Update app imports (showcase-web + docs)

## What to do

1. In `apps/showcase-web/src/components/BrandSwitcher.tsx`:
   - Update imports: change `@vlting/ui/design-tokens` or relative design-tokens paths → import from `@vlting/ui` (root barrel)
   - Rename: `Brand` → `Theme`, `defaultBrand` → `defaultTheme`, `funBrand` → `funTheme`, `poshBrand` → `poshTheme`, `shadcnBrand` → `shadcnTheme`
   - Rename component if needed: `BrandSwitcher` → `ThemeSwitcher` (file name too)
   - Update any parent that imports BrandSwitcher

2. In `apps/showcase-web/vite.config.ts`:
   - Remove any Vite alias for `design-tokens`
   - Keep aliases for react-native-web, react-native-svg-web, @vlting/ui

3. In `apps/docs/components/brand-switcher.tsx`:
   - Same import/rename changes as showcase-web
   - Rename file to `theme-switcher.tsx` if practical
   - Update any parent that imports it

4. In `apps/docs/` Vite/Next config:
   - Remove any design-tokens alias

5. Search for any other files in apps/ that reference `design-tokens` or `Brand` (as a type from design-tokens) and update.

## Checklist
- [ ] Update showcase-web BrandSwitcher imports + rename
- [ ] Update showcase-web vite.config.ts
- [ ] Update docs brand-switcher + rename
- [ ] Update docs config
- [ ] Search and fix remaining references

## Files touched
- apps/showcase-web/src/components/BrandSwitcher.tsx (+ rename)
- apps/showcase-web/vite.config.ts
- apps/docs/components/brand-switcher.tsx (+ rename)
- apps/docs/next.config.mjs (if has alias)
- Any parent files importing BrandSwitcher

## Acceptance criteria
- Zero references to `design-tokens` in apps/
- Zero references to `Brand` type (from design-tokens) in apps/
- BrandSwitcher renamed to ThemeSwitcher
- Vite aliases updated

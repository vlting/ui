# Commit History
- `62d1615` feat(chart): add AreaChart component with 10 variants
- `5939b6c` merge: feat(chart): add AreaChart component with 10 variants (#001)

---

<!-- auto-queue -->
<!-- target-branch: feat/chart-system/core-charts -->

# Segment 1: AreaChart Component

Create the AreaChart component with 10 variants using Victory Native.

## Scope
- `packages/components/Chart/AreaChart.tsx` (create)

## Context
- Stage 2 of Chart System epic (#40, sub-issue #42)
- Feature flag: `chart_system`
- Foundation from Stage 1: `Chart` (ChartContainer), `useChartContext()`, `ChartTooltip`, `ChartLegend`, `ChartConfig` type, `createChartTheme()`, color resolution utils

## Result
- Implemented AreaChart using SVG + react-native-svg with inline path math (d3-scale/d3-shape had no type declarations; victory-native v41+ uses Skia API incompatible with web)
- All 10 variants supported: default, gradient, stacked, expanded, step, axes, interactive, legend, linear, icons
- TypeScript compiles cleanly with `npx tsc --noEmit`
- Exports: `AreaChart`, `AreaChartProps`, `AreaChartVariant`

<!-- LAT: 2026-03-10T21:29:02Z -->
<!-- PID: 77812 -->
<!-- auto-queue -->
<!-- target-branch: fix/docs-polish/component-rendering -->
# Task: Fix component preview rendering with StlProvider wrapping

## Context
Component previews in the docs site render unstyled because they lack StlProvider context. After Task 001 adds StlProvider to the root providers, component previews should automatically inherit styling. However, we need to verify and fix any remaining issues.

The live examples system (`live-examples.tsx`) contains ~55 hardcoded component examples as raw JSX. These should now render styled since StlProvider is in the root provider chain.

## Files to modify
- `apps/docs/components/component-preview.tsx`
- `apps/docs/components/component-examples.tsx`
- `apps/docs/components/live-examples.tsx` (if needed)

## Implementation

1. **Verify component-preview.tsx**: The preview pane currently renders `{children}` directly. Since StlProvider is now at the root level, this should work. BUT — check if the preview container needs any STL-specific CSS class or attribute (like `data-stl` or similar). Add it if required.

2. **Check component-examples.tsx**: Ensure `getLiveExample()` calls work correctly with the StlProvider context. The examples should render styled and interactive.

3. **Check live-examples.tsx**:
   - Verify the example components import correctly from `@vlting/ui` or the packages directory
   - Fix any TypeScript cast issues that might hide rendering problems
   - Ensure all ~55 examples have valid JSX that renders meaningful content (not just empty wrappers)
   - Look for examples that might need specific props to render visually (e.g., charts need data, tables need rows)

4. **Test a few examples locally** by checking if the imports resolve:
   - Run `yarn typecheck` in the docs app or check imports manually
   - Verify the component exports from `@vlting/ui` match what live-examples.tsx imports

## Acceptance
- Component preview containers properly display styled @vlting/ui components
- Live examples render with correct styling (colors, spacing, borders, shadows)
- No empty or broken preview states for the component examples
- TypeScript types resolve correctly
- Commit with conventional commit message

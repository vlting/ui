<!-- LAT: 2026-03-18T16:30:00Z -->
<!-- PID: $PPID -->
<!-- target-branch: feat/library-buildout/content-components -->

# Task: Alert variant API cleanup

Council-approved changes to align Alert with canonical variant vocabulary.

## Context

Plan: `.claude/plans/zany-seeking-globe.md`

Alert currently has `outline | outlineMin | subtle | solid` variants + `floating` boolean.
After: `outline | subtle | solid` + `elevation: flat | raised`.

## Checklist

- [ ] In `Alert.tsx`: remove current `outline` variant (full-strength $9 borders)
- [ ] In `Alert.tsx`: rename `outlineMin` → `outline` in the `variant` object
- [ ] In `Alert.tsx`: update all compoundVariants referencing `outlineMin` → `outline`
- [ ] In `Alert.tsx`: replace `floating: { true: { boxShadow: '$md' } }` with `elevation: { flat: {}, raised: { boxShadow: '$md' } }`
- [ ] In `Alert.tsx`: update `defaultVariants` — add `elevation: 'flat'`
- [ ] In `Alert.spec.md`: update to reflect actual variant set: `solid | subtle | outline` + `elevation: flat | raised`

## Files

- `packages/components/Alert/Alert.tsx`
- `packages/components/Alert/Alert.spec.md`

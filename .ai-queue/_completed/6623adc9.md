<!-- LAT: 2026-03-10T20:58:42Z -->
<!-- PID: 77812 -->
<!-- auto-queue -->
<!-- target-branch: feat/foundation-cleanup/default-theme-packaging -->
# Task: Consolidate root exports — add StlProvider, injectBrandVars, getBrandStyleTag

## Context
The root `@vlting/ui` package (`src/index.ts`) exports `Provider` and `defaultBrand` but does NOT export:
- `StlProvider` from `@vlting/stl-react`
- `injectBrandVars` from `packages/design-tokens/brands/inject.ts`
- `getBrandStyleTag` from `packages/design-tokens/brands/inject.ts`
- `generatePalette` from `packages/design-tokens/brands/generate-palette.ts`
- `getTextColorStep` from `packages/design-tokens/brands/generate-palette.ts`

These are already exported from `packages/design-tokens/brands/index.ts` but not re-exported from root.

## Files to modify
- `src/index.ts` — ONLY this file

## Implementation
1. In the "Infrastructure" section at the top, add re-exports for `StlProvider` and its props:
   ```ts
   export { StlProvider } from '../packages/stl-react/src/providers/StlProvider'
   export type { StlProviderProps } from '../packages/stl-react/src/providers/StlProvider'
   ```

2. In the "Layer 0 — Design Tokens" section, add to the existing exports from `'../packages/design-tokens'`:
   - `injectBrandVars`
   - `getBrandStyleTag`
   - `generatePalette`
   - `getTextColorStep`

   These are already exported from `packages/design-tokens/brands/index.ts` → `packages/design-tokens/index.ts`, so they should be accessible.

3. Check that `packages/design-tokens/index.ts` actually re-exports these. If not, also add the re-export there. But ONLY modify `src/index.ts` and `packages/design-tokens/index.ts` if needed.

## Acceptance
- `import { StlProvider, injectBrandVars, getBrandStyleTag, generatePalette, getTextColorStep } from '@vlting/ui'` works (types resolve)
- No duplicate exports
- File compiles without errors
- Commit with conventional commit message

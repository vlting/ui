<!-- LAT: 2026-03-10T21:57:05Z -->
<!-- PID: 96331 -->
<!-- auto-queue -->
<!-- target-branch: fix/showcase-polish/builds-core -->
# Task: Fix all showcase-web TypeScript errors to make build pass

## Context
`yarn workspace @vlting/showcase-web build` fails with ~85 TypeScript errors. The build command is `tsc -b && vite build`. The errors are primarily:

1. **Compound component API mismatches** — Pages use `<Accordion>` directly but components export objects with `.Root`, `.Trigger`, `.Content` etc. Fix: use `<Accordion.Root>`, `<Accordion.Item>` etc.

2. **Missing Badge web export** — `Badge` only has `Badge.native.tsx`, not exported for web in `packages/primitives/index.ts`. Fix: check if Badge.tsx exists; if yes, export it. If not, create a simple Badge primitive or use a type assertion.

3. **Select API mismatch** — Pages expect `Select.Trigger`, `Select.Content` but actual API has different sub-components. Fix: read `packages/components/Select/` to find actual API and update page code.

4. **Hook type issues** — HooksPage.tsx references hooks that don't exist or have different signatures (useTransition, useListState, useSearch, useTabs, useToastQueue). Fix: remove or replace with actual hooks from `packages/hooks/`.

5. **Card/Dialog/other compound components** — Pages pass `children` directly but components expect compound usage (`<Card.Root>`, `<Card.Header>`, etc.). Fix: update to compound pattern.

## Files to modify
- ALL files in `apps/showcase-web/src/pages/` that have TypeScript errors
- `packages/primitives/index.ts` ONLY if Badge export is missing (to add it)

## Implementation Strategy

1. **First**: Run `cd apps/showcase-web && npx tsc -b --noEmit 2>&1` to get the exact error list.

2. **For each page with errors**: Read the page file AND the corresponding component source to understand the actual API:
   - Read `packages/components/Accordion/Accordion.tsx` (or index.ts) to see actual exports
   - Then fix the page to match

3. **For HooksPage**: Read `packages/hooks/index.ts` to see actual available hooks. Remove demos for hooks that don't exist. Keep demos for hooks that do exist but fix their usage.

4. **For Badge**: Check `packages/primitives/Badge.tsx` — if it exists for web, add it to the primitives index. If not, create a minimal web Badge or remove Badge demos from pages.

5. **After all fixes**: Run `npx tsc -b --noEmit` again to verify zero errors. Then run `npx vite build` to verify the Vite build succeeds.

## Key Principle
Do NOT change the component library code to match the pages. Change the PAGES to match the component library's actual API. The library is the source of truth.

## Acceptance
- `yarn workspace @vlting/showcase-web build` succeeds (exit code 0)
- Zero TypeScript errors
- Vite build produces output in `apps/showcase-web/dist/`
- Commit with conventional commit message
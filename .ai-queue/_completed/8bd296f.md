<!-- LAT: 2026-03-10T04:43:59Z -->
<!-- PID: 28592 -->
<!-- auto-queue -->
<!-- target-branch: feat/component-quality/visual-polish -->
# Task: Visual polish pass — Button, Card, Input, Alert, Badge

## Goal
Polish visual quality of top-tier components to match shadcn/ui patterns.

## Instructions

1. Read the QUALITY_BASELINE.md if it exists in packages/
2. Read the current state of each component

3. For each component, apply visual polish:

### Button (`packages/components/Button/Button.tsx`)
- Ensure consistent padding across sizes (sm: 8px 12px, md: 8px 16px, lg: 12px 24px)
- Ensure font-size matches size variant
- Verify variant colors: default (neutral bg), destructive (red), outline (border-only), ghost (transparent), link (no bg)
- Smooth hover/press transitions

### Card (`packages/components/Card/Card.tsx`)
- Subtle border, rounded corners (radius-3 or 8px equivalent)
- Proper shadow token usage (shadow-sm)
- Consistent padding (Card.Header, Card.Content, Card.Footer)
- Dark mode: slightly elevated surface color

### Input (`packages/components/Input/Input.tsx`)
- Height 40px (md), consistent with Button sizing
- Border: 1px solid, neutral border color
- Placeholder color: muted/color8
- Error state: red/error border color

### Alert (`packages/components/Alert/Alert.tsx`)
- Icon + title + description layout
- Variant colors: default (neutral), destructive (red bg/border)
- Proper spacing between icon and text

### Badge (`packages/components/Badge/Badge.tsx`)
- Small, inline element
- Variant colors: default, secondary, outline, destructive
- Consistent padding (2px 8px), rounded-full or radius-2

4. Do NOT change component APIs — visual CSS only.
5. Use STL tokens where possible.

## Key files
- Listed component files

<!-- LAT: 2026-03-09T14:51:52Z -->
<!-- PID: 5792 -->
<!-- auto-queue -->
<!-- depends-on: 001, 002, 003, 004, 005, 006 -->
<!-- target-branch: feat/stl-primitives/text-visual-primitives -->
# Task: Integration — update barrel exports and clean test imports

Final integration task after all primitives are migrated.

## Files to modify
- `packages/primitives/index.ts` — ensure all exports are clean, no Tamagui type leaks
- `packages/primitives/Box.test.tsx` — remove `@ts-expect-error` line
- `packages/primitives/Stack.test.tsx` — remove `@ts-expect-error` line
- `packages/primitives/Spacer.test.tsx` — remove `@ts-expect-error` line
- `packages/primitives/Divider.test.tsx` — remove `@ts-expect-error` line
- `packages/primitives/Separator.test.tsx` — remove `@ts-expect-error` line

## Implementation

### index.ts cleanup
Read `packages/primitives/index.ts` and verify:
1. All 18 primitives are exported (Box, Stack, VStack, HStack, Spacer, Divider, Separator, AspectRatio, Text, Heading, Badge, Skeleton, Spinner, Kbd, Icon, Label, VisuallyHidden, Portal)
2. All prop types are exported (BoxProps, TextProps, HeadingProps, BadgeProps, etc.)
3. No `tamagui` or `@tamagui` in any import
4. No `GetProps` type import

### Test file cleanup
For each test file listed above:
1. Remove the `// @ts-expect-error Tamagui v2 RC GetProps bug` comment (usually line 3)
2. The line after it imports from `../../src/__test-utils__/render` — keep the import, just remove the `@ts-expect-error` comment above it
3. Verify the test file has no other `@ts-expect-error` or `as any`

### Verification
After changes:
1. Run `grep -r "@ts-expect-error" packages/primitives/` — should return zero results
2. Run `grep -r "from 'tamagui'" packages/primitives/` — should return zero results
3. Run `grep -r "from '@tamagui" packages/primitives/` — should return zero results

## Acceptance criteria
- Zero `@ts-expect-error` across ALL files in packages/primitives/
- Zero `tamagui` or `@tamagui` imports across ALL files in packages/primitives/
- All 18 primitives + their prop types exported from index.ts
- TypeScript compiles

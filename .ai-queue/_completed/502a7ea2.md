<!-- LAT: 2026-03-10T21:40:16Z -->
<!-- PID: 77812 -->
<!-- auto-queue -->
<!-- target-branch: feat/docs-polish/code-blocks-ui -->
# Task: Verify and fix code block padding + copy button overlap

## Context
The copy button in code blocks is positioned `absolute right-2 top-2`. Need to verify this doesn't overlap with code text across various code lengths and viewports. Also check import boxes and all code sample locations.

## Files to check/modify
- `apps/docs/components/code-block.tsx` — padding and positioning
- `apps/docs/components/copy-button.tsx` — button styling
- `apps/docs/app/globals.css` — if CSS overrides needed

## Implementation

1. Read `apps/docs/components/code-block.tsx` to check:
   - The container's padding (especially right padding)
   - The code text area's max-width or overflow behavior
   - The copy button positioning relative to code

2. If the code block container uses `p-4` (16px) and the copy button is at `right-2` (8px) with the button being ~28px wide, the code needs at least `pr-10` (40px) right padding to avoid overlap.

3. Check that the Shiki-generated `<pre>` element has proper right padding:
   - Look for existing Tailwind overrides like `[&_pre]:!p-0` that might strip padding
   - If stripped, add right padding back: `[&_pre]:!pr-10` or similar

4. Verify import boxes (`app/docs/components/[name]/page.tsx`) have the copy button visible and non-overlapping.

5. Test edge cases:
   - Very long single-line code (should horizontally scroll, copy button stays visible)
   - Multi-line code blocks (copy button should stay in top-right)
   - Short code (copy button should not look awkward)

## Acceptance
- Copy button visible on all code blocks without overlapping code text
- Right padding is sufficient (minimum 40px right of code text to button)
- Import boxes have working copy buttons
- Code blocks scroll horizontally for long lines
- Commit with conventional commit message
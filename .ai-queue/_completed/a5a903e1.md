<!-- LAT: 2026-03-17T06:38:32Z -->
<!-- PID: 30766 -->
<!-- worktree: .worktrees/q-005 -->
<!-- branch: q-005 -->
<!-- auto-queue -->
<!-- target-branch: feat/library-buildout/content-components -->
<!-- depends-on: 001 -->
<!-- depends-on: 002 -->
<!-- depends-on: 003 -->
<!-- depends-on: 004 -->
# Task: Integration — jest.config, playground, Badge export

Wire up all 4 new components after they've been implemented.

## Files to modify
- `jest.config.js` — remove 4 components from testPathIgnorePatterns
- `packages/components/index.ts` — add Badge export
- `apps/playground/src/App.tsx` — add playground sections for Card, Avatar, Badge, Item

## 1. jest.config.js

Remove these 4 lines from the `testPathIgnorePatterns` array:
```
'packages/components/Avatar/',
'packages/components/Card/',
'packages/components/Item/',
```

And do NOT add Badge (it was never in the ignore list since it's new).

## 2. packages/components/index.ts

Add Badge export. Find where the other component exports are and add:
```tsx
export type { BadgeProps } from './Badge'
export { Badge } from './Badge'
```

## 3. apps/playground/src/App.tsx

Add sections for each component after the existing Alert/Progress/Spinner/Empty sections. Follow the existing playground patterns.

### Card section
```tsx
<Section title="Card">
  <DemoCard>
    <Card>
      <Card.Header>
        <Card.Title>Card Title</Card.Title>
        <Card.Description>Card description text</Card.Description>
      </Card.Header>
      <Card.Content>
        <p style={{ margin: 0 }}>Card content goes here.</p>
      </Card.Content>
      <Card.Footer>
        <Button size="sm">Action</Button>
      </Card.Footer>
    </Card>
  </DemoCard>
  <DemoCard title="Elevated">
    <Card elevated>
      <Card.Header><Card.Title>Elevated Card</Card.Title></Card.Header>
      <Card.Content><p style={{ margin: 0 }}>Shadow instead of border.</p></Card.Content>
    </Card>
  </DemoCard>
  <DemoCard title="Interactive">
    <Card interactive onPress={() => {}}>
      <Card.Content><p style={{ margin: 0 }}>Click me</p></Card.Content>
    </Card>
  </DemoCard>
  <DemoCard title="Sizes">
    {(['sm', 'md', 'lg'] as const).map(size => (
      <Card key={size} size={size} stl={{ mb: '$8' }}>
        <Card.Content><p style={{ margin: 0 }}>Size: {size}</p></Card.Content>
      </Card>
    ))}
  </DemoCard>
</Section>
```

### Avatar section
```tsx
<Section title="Avatar">
  <DemoCard title="Sizes">
    <div style={{ display: 'flex', gap: 12, alignItems: 'center' }}>
      {(['sm', 'md', 'lg', 'xl'] as const).map(size => (
        <Avatar key={size} size={size} fallback="JD" />
      ))}
    </div>
  </DemoCard>
</Section>
```

### Badge section
```tsx
<Section title="Badge">
  <DemoCard title="Solid">
    <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
      {(['primary', 'secondary', 'neutral', 'success', 'warning', 'error', 'info'] as const).map(theme => (
        <Badge key={theme} theme={theme} variant="solid">{theme}</Badge>
      ))}
    </div>
  </DemoCard>
  <DemoCard title="Subtle">
    <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
      {(['primary', 'secondary', 'neutral', 'success', 'warning', 'error', 'info'] as const).map(theme => (
        <Badge key={theme} theme={theme} variant="subtle">{theme}</Badge>
      ))}
    </div>
  </DemoCard>
  <DemoCard title="Outline">
    <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
      {(['primary', 'secondary', 'neutral', 'success', 'warning', 'error', 'info'] as const).map(theme => (
        <Badge key={theme} theme={theme} variant="outline">{theme}</Badge>
      ))}
    </div>
  </DemoCard>
  <DemoCard title="Sizes">
    <div style={{ display: 'flex', gap: 8, alignItems: 'center' }}>
      {(['sm', 'md', 'lg'] as const).map(size => (
        <Badge key={size} size={size}>size: {size}</Badge>
      ))}
    </div>
  </DemoCard>
</Section>
```

### Item section
```tsx
<Section title="Item">
  <DemoCard>
    <Item>
      <Item.Leading>📄</Item.Leading>
      <Item.Content>
        <Item.Title>Item title</Item.Title>
        <Item.Description>Description text here</Item.Description>
      </Item.Content>
      <Item.Trailing>→</Item.Trailing>
    </Item>
  </DemoCard>
  <DemoCard title="Interactive">
    <Item interactive>
      <Item.Content>
        <Item.Title>Clickable item</Item.Title>
        <Item.Description>With hover and focus states</Item.Description>
      </Item.Content>
    </Item>
  </DemoCard>
  <DemoCard title="Sizes">
    {(['sm', 'md', 'lg'] as const).map(size => (
      <Item key={size} size={size}>
        <Item.Content><Item.Title>Size: {size}</Item.Title></Item.Content>
      </Item>
    ))}
  </DemoCard>
</Section>
```

Add imports at the top: `import { Avatar, Badge, Card, Item } from '@vlting/ui'`

NOTE: Adapt the playground patterns to match whatever Section/DemoCard helpers exist in the file. If they don't exist, use simple styled containers. Check the existing playground code for the right pattern.

## Acceptance criteria
- `npm test` runs Card, Avatar, Badge, Item tests (no longer ignored)
- All 4 tests pass
- Badge is exported from @vlting/ui
- Playground shows all 4 components with variant demos
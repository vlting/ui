<!-- LAT: 2026-03-10T20:18:59Z -->
<!-- PID: 96331 -->
<!-- auto-queue -->
<!-- target-branch: chore/foundation-cleanup/tamagui-removal -->
# Task: Kitchen-Sink Example Migration

Migrate the `examples/kitchen-sink/` app from Tamagui to @vlting/ui.

## Context
The kitchen-sink imports `Text, View, XStack, YStack` from `'tamagui'` and uses `createTamagui` for initialization. It also uses `@tamagui/lucide-icons`. All of these need to be replaced with @vlting/ui equivalents.

## Mapping
| Tamagui | @vlting/ui |
|---------|-----------|
| `import { Text, View, XStack, YStack } from 'tamagui'` | `import { Text, Box, HStack, VStack } from '@vlting/ui'` |
| `import { createTamagui } from 'tamagui'` | Remove — use STL provider instead |
| `@tamagui/lucide-icons` | `import { DynamicIcon } from '@vlting/ui'` or specific icons from `@vlting/ui` |
| `<XStack>` | `<HStack>` |
| `<YStack>` | `<VStack>` |
| `<View>` | `<Box>` |
| Tamagui style props (`$color`, `$background`, etc.) | CSS custom properties via STL |

## Files to modify (all in examples/kitchen-sink/)

### 1. `package.json`
- Remove `@tamagui/lucide-icons` dependency
- Remove `tamagui` dependency if present
- Ensure `@vlting/ui` is listed as dependency

### 2. `src/brands.ts`
- Remove `createTamagui` import
- Replace brand config factory with STL-based config
- Use `@vlting/ui` design tokens (defaultBrand, funBrand, etc.)

### 3. `src/layouts/BrandLayout.tsx`
- Replace `import { Text, View, XStack, YStack } from 'tamagui'` with @vlting/ui primitives
- Remove the `AnyFC` type cast workaround (not needed with STL)
- Update all JSX to use new component names

### 4. `src/components/Section.tsx`
- Replace tamagui imports with @vlting/ui

### 5. All page files (`src/pages/*.tsx`):
- ButtonsPage, ComposedPage, DataDisplayPage, FormsPage, HomePage, HooksPage, MenusPage, OverlaysPage, PrimitivesPage
- Replace all `import { Text, View, XStack, YStack } from 'tamagui'`
- Update JSX component names
- Replace Tamagui token props (`$color3`, `$background`, etc.) with STL CSS variables

### 6. `vite.config.ts`
- Remove any tamagui-specific Vite plugins or config
- Ensure vanilla-extract plugin is configured

### 7. Check `tsconfig.json` in examples/kitchen-sink/
- Remove tamagui path aliases

## Important
- The kitchen-sink uses `TamaguiProvider` — replace with `StlProvider` from `@vlting/ui`
- Keep all existing functionality working — brand switching, theme toggling, all demo pages
- Use `@vlting/ui` primitives (Box, VStack, HStack, Text, Heading) and CSS custom properties for styling
- For icons, use the @vlting/ui icon system (DynamicIcon or direct icon imports)

## Acceptance criteria
- Zero imports from 'tamagui' or '@tamagui/*'
- App renders and runs (`yarn dev:kitchen-sink`)
- All pages accessible, brand switching works

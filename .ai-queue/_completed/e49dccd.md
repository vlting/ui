# Commit History
- `3c0a783` feat(primitives): add Label, VisuallyHidden, Badge, Skeleton, Separator, AspectRatio
- `e49dccd` feat(primitives): add 6 missing primitives (merge)

---

<!-- auto-queue -->
# Task 003: Implement Missing Primitives

## Result
6 new primitives added:
- Label — form label with htmlFor, size, required indicator
- VisuallyHidden — sr-only content
- Badge — inline status indicator (solid/outline/subtle, 5 tones, 3 sizes)
- Skeleton — loading placeholder with animation
- Separator — semantic divider with a11y role
- AspectRatio — fixed ratio container

All exported from barrel and src/index.ts. Build verified clean.

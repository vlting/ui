<!-- LAT: 2026-03-17T06:36:59Z -->
<!-- PID: 30766 -->
<!-- worktree: .worktrees/q-004 -->
<!-- branch: q-004 -->
<!-- auto-queue -->
<!-- target-branch: feat/library-buildout/content-components -->
# Task: Item component

Replace the Item stub with a real `styled()` implementation.

## Files to modify
- `packages/components/Item/Item.tsx` — full rewrite
- `packages/components/Item/index.ts` — update type exports

## API contract (from tests)

Tests use `<Item>` directly (NOT `<Item.Root>`), with sub-components via dot notation:
```tsx
<Item size="md" interactive data-testid="item">
  <Item.Leading><span>icon</span></Item.Leading>
  <Item.Content>
    <Item.Title>Title text</Item.Title>
    <Item.Description>Description text</Item.Description>
  </Item.Content>
  <Item.Trailing><span>action</span></Item.Trailing>
</Item>
```

Optional sub-components: Leading, Trailing, Description are all optional.

## Implementation

Import `styled` from `../../stl-react/src/config`.

### ItemRoot = styled('div', { ... })
- **stl:** `display: 'flex'`, `alignItems: 'center'`, `gap: '$12'`, `fontFamily: '$body'`
- **variants:**
  - `size`: sm (`minHeight: '36px'`, `py: '$4'`, `px: '$8'`), md (`minHeight: '44px'`, `py: '$8'`, `px: '$12'`), lg (`minHeight: '52px'`, `py: '$12'`, `px: '$16'`)
  - `interactive`: true → `cursor: 'pointer'`, `:interact`: `bg: '$color3'`, `:pressed`: `bg: '$color4'`, `:focus`: `outlineWidth: '2px'`, `outlineStyle: 'solid'`, `outlineColor: '$outlineColor'`, `outlineOffset: '2px'`, `borderRadius: '$field'`, `lowMotion`: `:pressed`: `transition: 'none'`
- **defaultVariants:** `size: 'md'`
- **mapProps:** When `interactive` is truthy → add `tabIndex: 0`
- **styleName:** `'Item'`

### Sub-components
- `ItemLeading` = `styled('div', { stl: { display: 'flex', alignItems: 'center', justifyContent: 'center', flexShrink: '0' }, styleName: 'ItemLeading' })`
- `ItemContent` = `styled('div', { stl: { display: 'flex', flexDirection: 'column', gap: '$2', flex: '1', minWidth: '0' }, styleName: 'ItemContent' })`
- `ItemTitle` = `styled('span', { stl: { fontWeight: '$500', fontSize: '$p', color: '$neutralText3' }, styleName: 'ItemTitle' })`
- `ItemDescription` = `styled('span', { stl: { fontSize: '$small', color: '$neutralText4' }, styleName: 'ItemDescription' })`
- `ItemTrailing` = `styled('div', { stl: { display: 'flex', alignItems: 'center', justifyContent: 'center', flexShrink: '0' }, styleName: 'ItemTrailing' })`

### Export
```tsx
export const Item = Object.assign(ItemRoot, {
  Leading: ItemLeading,
  Content: ItemContent,
  Title: ItemTitle,
  Description: ItemDescription,
  Trailing: ItemTrailing,
})

export type ItemProps = ComponentPropsWithRef<typeof ItemRoot>
export type ItemLeadingProps = ComponentPropsWithRef<typeof ItemLeading>
export type ItemContentProps = ComponentPropsWithRef<typeof ItemContent>
export type ItemTitleProps = ComponentPropsWithRef<typeof ItemTitle>
export type ItemDescriptionProps = ComponentPropsWithRef<typeof ItemDescription>
export type ItemTrailingProps = ComponentPropsWithRef<typeof ItemTrailing>
```

## Acceptance criteria
- All Item tests pass (don't modify test file)
- All styling via STL tokens — no hardcoded colors/sizes
- `styleName` on every styled() call
- Interactive variant has focus ring + hover/press states
- `lowMotion` block on interactive transitions
- Consumer sets role (not hardcoded) — per spec, Item is a generic layout
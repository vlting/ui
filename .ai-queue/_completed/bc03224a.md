<!-- LAT: 2026-03-10T21:57:14Z -->
<!-- PID: 77812 -->
<!-- auto-queue -->
<!-- target-branch: fix/showcase-polish/builds-core -->
# Task: Verify and fix showcase-native Expo build

## Context
The showcase-native app (`apps/showcase-native/`) uses Expo 53 with React Native 0.81.5. Need to verify it builds and fix any issues.

## Files to modify
- Files in `apps/showcase-native/` ONLY

## Implementation

1. Check if Expo CLI is available: `npx expo --version`

2. Try `cd apps/showcase-native && npx expo export --platform web 2>&1` (web export as a proxy for build verification). If Expo CLI isn't available, at minimum verify TypeScript compilation: `npx tsc --noEmit`

3. Fix any TypeScript errors found — these would be similar to showcase-web (compound component API mismatches, hook type issues).

4. Verify the app structure:
   - Read `apps/showcase-native/app/` for screen files
   - Check that imports from `@vlting/ui` resolve correctly
   - Verify StlProvider is wrapping the app

5. Read the key screen files and verify they use correct component APIs.

## Acceptance
- TypeScript compilation succeeds for showcase-native
- All screen files use correct component APIs
- No broken imports
- Commit with conventional commit message
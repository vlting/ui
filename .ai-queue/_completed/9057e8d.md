<!-- LAT: 2026-03-09T15:41:36Z -->
<!-- PID: 6276 -->
<!-- auto-queue -->
<!-- target-branch: feat/stl-components/compound-components -->
# Task: Migrate Dialog, AlertDialog, Sheet to stl + headless hooks

Replace Tamagui overlay components with native HTML + stl styling + useDisclosure from stl-headless.

## Migration pattern
- Import `styled` from `'../stl-react/src/config'`
- Import `useDisclosure` from `'../../packages/stl-headless/src'`
- Use React portals (`createPortal`) for overlay rendering
- Use CSS transitions for animations
- Proper ARIA: `role="dialog"`, `aria-modal`, `aria-labelledby`, `aria-describedby`

## Files to modify

### `packages/components/Dialog/Dialog.tsx`
- Replace `Dialog` from Tamagui with custom implementation
- Use `useDisclosure` for open/close state
- Use `createPortal(document.body)` for overlay
- Compound: Root, Trigger, Overlay, Content, Title, Description, Close, Header, Footer
- Focus trap: use `onKeyDown` to trap Tab key within dialog
- Escape key to close

### `packages/components/AlertDialog/AlertDialog.tsx`
- Same pattern as Dialog but with `role="alertdialog"`
- Compound: Root, Trigger, Overlay, Content, Title, Description, Footer, Cancel, Action

### `packages/components/Sheet/Sheet.tsx`
- Bottom sheet pattern: positioned at bottom with CSS transform
- Use `useDisclosure` for open/close
- CSS transition for slide-up animation
- Compound: Root, Overlay, Handle, Frame, ScrollView
- Touch/drag handling can be simplified to CSS-only for now

## Acceptance criteria
- Zero Tamagui imports in all 3 files
- Proper ARIA attributes (role, aria-modal, aria-labelledby)
- Portal rendering works
- Compound sub-component APIs preserved

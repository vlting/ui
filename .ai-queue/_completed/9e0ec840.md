<!-- LAT: 2026-03-18T12:00:00Z -->
<!-- PID: ${PPID} -->
<!-- auto-queue -->
<!-- target-branch: feat/library-buildout/content-components -->

# Task: Update Item.spec.md to match implementation

## Files
- `packages/components/Item/Item.spec.md`

## Context
- Spec follows implementation, not the other way around
- Implementation uses `$field` for radius (no `$item` token exists)
- Implementation uses `Item.Leading`/`Item.Trailing` (not `Item.Media`/`Item.Actions`)
- Only `theme`, `variant`, `size`, `interactive` axes are implemented now
- `disabled`, `selected`, `divider` are deferred — spec should note them as planned/future

## Instructions

1. **Read the current Item.spec.md** fully.

2. **Update radius references**: Replace `$item` with `$field` throughout the spec.

3. **Update sub-component names**: Replace `Item.Media` → `Item.Leading`, `Item.Actions` → `Item.Trailing` to match the implementation.

4. **Mark deferred axes**: Add a note that `disabled`, `selected`, `divider` are planned but not yet implemented. Keep their spec sections but mark them as `[Planned]`.

5. **Verify the variant styling contract matches what was implemented** in task 011:
   - default: transparent bg, no border
   - subtle: bg `$<theme>3`, radius `$field`
   - outline: transparent bg, border `$<theme>5`, borderWidth `$widthMin`, radius `$field`

## Checklist
- [ ] Read current spec
- [ ] Update $item → $field references
- [ ] Update sub-component names to match impl
- [ ] Mark deferred axes as [Planned]
- [ ] Verify styling contract accuracy

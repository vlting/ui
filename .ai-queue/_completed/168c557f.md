<!-- LAT: 2026-03-10T20:30:01Z -->
<!-- PID: 77812 -->
<!-- auto-queue -->
<!-- target-branch: chore/foundation-cleanup/tamagui-removal -->
# Task: Documentation & Constitution Update

## Progress
- [x] AI_CONSTITUTION.md
- [x] src/provider/Provider.spec.md
- [x] packages/QUALITY_BASELINE.md, FRONTEND_QUALITY.contract.md
- [x] apps/docs/ (5 files)
- [x] .claude/skills/frontend-quality/SKILL.md
- [ ] All packages/ .spec.md files (agent)
- [ ] docs/ markdown files (agent)
- [ ] Final verification

Remove all Tamagui references from documentation, constitution files, and component specs.

## Files to modify

### 1. `AI_CONSTITUTION.md` (root)
This is the critical governance document. Replace ALL Tamagui references:
- "cross-platform design system built on Tamagui" → "cross-platform design system built on @vlting/stl"
- Tamagui `View`/`XStack`/`YStack` warnings → update to STL primitives (Box, HStack, VStack)
- `tag` prop section about Tamagui v2 RC → update to reflect STL styled() behavior
- `styledHtml()` section → update or remove if not applicable to STL
- Vite config examples with Tamagui-specific `process.env` defines → update for STL
- TypeScript paths examples → update for @vlting/stl

### 2. `src/provider/Provider.spec.md`
- "Wraps TamaguiProvider from Tamagui" → "Wraps StlProvider from @vlting/stl-react"
- "optional Tamagui configuration object" → "optional STL configuration"

### 3. `apps/docs/app/docs/migration/page.tsx`
- Update all code examples: remove `createTamagui()`, Tamagui imports
- Update install command: remove tamagui package
- Update style prop migration examples to use STL tokens/CSS vars
- This is the user-facing migration guide — must be accurate

### 4. `apps/docs/app/docs/changelog/page.tsx`
- Update any Tamagui references in changelog entries

### 5. `apps/docs/app/page.tsx`
- Check for any Tamagui branding on the homepage

### 6. `apps/docs/lib/registry.ts`
- Check for any Tamagui references in component registry descriptions

### 7. `apps/docs/components/playground.tsx`
- Check for Tamagui references

### 8. `docs/migration-guide.md` (root docs/)
- Update or remove Tamagui v2 migration content

### 9. `packages/QUALITY_BASELINE.md`
- Replace Tamagui conventions with STL conventions

### 10. `packages/FRONTEND_QUALITY.contract.md`
- Replace Tamagui references with STL

### 11. All `.spec.md` files in `packages/components/` and `packages/primitives/`
Run: `grep -rl "tamagui\|Tamagui" packages/components/ packages/primitives/ --include="*.spec.md"`
For EACH file found, replace:
- "Tamagui style props" → "STL style props" or "CSS custom properties"
- "Tamagui tokens" → "STL tokens" or "design tokens"
- "Tamagui component" → "STL component" or "@vlting/ui component"
- "Tamagui" (generic) → "STL" or "@vlting/stl"

### 12. `DESIGN_CONSTITUTION.md` (if it has Tamagui refs)
- Update to reflect STL architecture

## Acceptance criteria
- Zero "tamagui" or "Tamagui" strings in any .md file under packages/, src/, apps/docs/, or root (excluding .ai-*, node_modules, dist)
- Documentation accurately reflects the STL architecture
- No broken links or references

<!-- LAT: 2026-03-11T12:00:00Z -->
<!-- PID: $PPID -->
<!-- auto-queue -->

# Task: Add StlProvider to showcase-web

## Checklist
- [ ] Create worktree and branch
- [ ] Add StlProvider wrapper to main.tsx
- [ ] Commit and merge
- [ ] Cleanup

## Context
`apps/showcase-web/src/main.tsx` is missing the `StlProvider` wrapper that other apps use (e.g. `apps/docs/components/providers.tsx`). Without it, theme context (color mode, tokens, etc.) isn't available at the root.

## What to do
Wrap the app in `<StlProvider>` in `apps/showcase-web/src/main.tsx`:
- Import `StlProvider` from `@vlting/stl-react`
- Wrap `<BrowserRouter><App /></BrowserRouter>` with `<StlProvider defaultColorMode="light">`
- Follow the pattern from `apps/docs/components/providers.tsx`

## Files
- `apps/showcase-web/src/main.tsx`

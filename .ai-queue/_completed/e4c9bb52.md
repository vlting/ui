<!-- LAT: 2026-03-18T22:16:26Z -->
<!-- PID: 7435 -->
<!-- auto-queue -->
<!-- target-branch: feat/library-buildout/content-components -->

# Task: Reduce Item typography sizes for sm and md

## Context

`packages/components/Item/Item.tsx` — Item.Title uses `fontSize: '$p'` (16px) globally with no size-variant differentiation. At `sm` (minHeight $36), 16px title is visually too large.

## Available fontSize tokens

- `$12` = 12px, `$13` = 13px, `$14` = 14px, `$15` = 15px, `$p` = 16px

## Changes

**File:** `packages/components/Item/Item.tsx`

1. Add `size` variants to `ItemTitle` so font size scales with the Item size:
   - `sm`: `fontSize: '$13'`
   - `md`: `fontSize: '$14'`
   - (lg remains `$p` / 16px — the current default)
   - Set `defaultVariants: { size: 'md' }`

2. Similarly scale `ItemDescription`:
   - `sm`: `fontSize: '$12'`
   - `md`: `fontSize: '$12'` (already 13, bring to 12 for md; keep 13 for lg)
   - Or keep description as-is if it already looks fine — use judgment

3. Thread the `size` prop from `ItemRoot` down to `ItemTitle` and `ItemDescription` in the component composition. Since these are compound sub-components (not auto-wired), this may require adding a context or manually passing the size variant. Check how other components in the codebase propagate parent variants to children — follow the same pattern.

4. If STL doesn't support parent→child variant propagation, an alternative: use compoundVariants on `ItemRoot` to set CSS custom properties or nest child selectors. Check existing patterns first.

## Checklist

- [ ] Research how other components propagate size variants to children
- [ ] Add size variants to ItemTitle with appropriate fontSize tokens
- [ ] Optionally adjust ItemDescription sizing
- [ ] Verify the component builds without errors
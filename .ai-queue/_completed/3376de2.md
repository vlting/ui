<!-- auto-queue -->
<!-- target-branch: feat/chart-system/chart-foundation -->

# Commit History
- `4783d50` feat(chart): add ChartContainer component with context and responsive sizing
- `3376de2` merge into feat/chart-system/chart-foundation

# Segment 2: ChartContainer Component

Stage 1 of Chart System epic (#40, sub-issue #41). Feature flag: `chart_system`.

## Scope

- `packages/components/Chart/Chart.tsx` (CREATE)

## Instructions

Create the main ChartContainer component, exported as `Chart` for external use.

### File: `packages/components/Chart/Chart.tsx`

This component wraps all chart types and provides:
- Responsive sizing (measures parent container)
- Theme integration (resolves Tamagui tokens → Victory theme)
- Chart context for child components
- Accessibility (role="img", aria-label)

**Implementation:**

```tsx
import React, { createContext, useContext, useRef, useState, useEffect, useMemo } from 'react'
import { useTheme } from 'tamagui'
import type { ChartConfig } from './types'
import { createChartTheme } from './theme'
import { getSeriesColors } from './utils'

// -- Chart Context --

interface ChartContextValue {
  config: ChartConfig
  resolvedColors: Record<string, string>
  victoryTheme: ReturnType<typeof createChartTheme>
  dimensions: { width: number; height: number }
}

const ChartContext = createContext<ChartContextValue | null>(null)

export function useChartContext(): ChartContextValue {
  const ctx = useContext(ChartContext)
  if (!ctx) {
    throw new Error('useChartContext must be used within a <Chart> component')
  }
  return ctx
}

// -- Chart Component --

export interface ChartContainerProps {
  /** Chart configuration mapping series keys to labels/colors */
  config: ChartConfig
  /** Chart content (chart type components) */
  children: React.ReactNode
  /** Accessibility label describing the chart */
  accessibilityLabel: string
  /** Explicit width in pixels (overrides responsive sizing) */
  width?: number
  /** Explicit height in pixels (overrides responsive sizing, default: 350) */
  height?: number
}

const DEFAULT_HEIGHT = 350

export function Chart({
  config,
  children,
  accessibilityLabel,
  width: explicitWidth,
  height: explicitHeight,
}: ChartContainerProps) {
  const containerRef = useRef<HTMLDivElement>(null)
  const [measuredWidth, setMeasuredWidth] = useState(0)
  const theme = useTheme()

  // Responsive sizing — only measure if no explicit width
  useEffect(() => {
    if (explicitWidth != null) return

    const el = containerRef.current
    if (!el) return

    // Initial measurement
    setMeasuredWidth(el.clientWidth)

    // Observe resize
    if (typeof ResizeObserver !== 'undefined') {
      const observer = new ResizeObserver((entries) => {
        for (const entry of entries) {
          setMeasuredWidth(entry.contentRect.width)
        }
      })
      observer.observe(el)
      return () => observer.disconnect()
    }
  }, [explicitWidth])

  // Resolve theme tokens to hex values for chart theming
  const resolvedTokens = useMemo(() => ({
    background: theme.background?.val || '#ffffff',
    color: theme.color?.val || '#111111',
    colorSubtitle: (theme as Record<string, { val?: string }>).color8?.val || '#6a6a6a',
    borderColor: theme.borderColor?.val || '#e8e8e8',
    color4: (theme as Record<string, { val?: string }>).color4?.val || '#ededed',
  }), [theme])

  const victoryTheme = useMemo(
    () => createChartTheme(resolvedTokens),
    [resolvedTokens]
  )

  const resolvedColors = useMemo(
    () => {
      // Build a flat lookup from theme for token resolution
      const themeFlat: Record<string, string> = {}
      for (const [key, val] of Object.entries(theme)) {
        if (val && typeof val === 'object' && 'val' in val && typeof val.val === 'string') {
          themeFlat[key] = val.val
        }
      }
      return getSeriesColors(config, themeFlat)
    },
    [config, theme]
  )

  const finalWidth = explicitWidth ?? measuredWidth
  const finalHeight = explicitHeight ?? DEFAULT_HEIGHT

  const contextValue = useMemo<ChartContextValue>(
    () => ({
      config,
      resolvedColors,
      victoryTheme,
      dimensions: { width: finalWidth, height: finalHeight },
    }),
    [config, resolvedColors, victoryTheme, finalWidth, finalHeight]
  )

  return (
    <div
      ref={containerRef}
      role="img"
      aria-label={accessibilityLabel}
      style={{
        width: explicitWidth != null ? explicitWidth : '100%',
        height: finalHeight,
        position: 'relative',
      }}
    >
      <ChartContext.Provider value={contextValue}>
        {finalWidth > 0 ? children : null}
      </ChartContext.Provider>
    </div>
  )
}
```

**Key decisions:**

1. **Native `<div>` for container** — Tamagui v2 RC `tag` prop doesn't change the rendered element. Using a native `<div>` ensures `role="img"` and `aria-label` are properly rendered.

2. **ResizeObserver for responsive sizing** — Only runs on web. On React Native, explicit width/height should always be provided (RN doesn't have ResizeObserver). The component renders nothing until width > 0 to avoid flash of incorrectly-sized chart.

3. **Theme resolution** — Uses `useTheme()` from Tamagui to get resolved token values. The `.val` property on theme tokens gives the actual CSS value. Build a flat `Record<string, string>` for the `getSeriesColors` utility.

4. **Memoization** — All derived values are memoized to prevent unnecessary Victory re-renders.

5. **Context-based architecture** — Child chart components (AreaChart, BarChart, etc.) consume `useChartContext()` to get dimensions, theme, and colors without prop drilling.

## Verification

- `Chart.tsx` compiles without TypeScript errors
- Component uses native `<div>` (not Tamagui View)
- `role="img"` and `aria-label` are on the container element
- `useChartContext()` throws when used outside `<Chart>`
- ResizeObserver is used for responsive sizing (with cleanup)
- All derived values are properly memoized
- No hardcoded colors or spacing (all from tokens)

## Task Progress
<!-- lat: 2026-03-02T05:44:44Z -->
<!-- agent-pid: 22715 -->
<!-- worktree: .worktrees/q-002 -->
<!-- branch: q/002 -->

### Checklist
- [x] Create worktree from feat/chart-system/chart-foundation
- [x] Create `packages/components/Chart/Chart.tsx`
- [x] Verify TypeScript compilation
- [ ] **ACTIVE** → Merge back to target branch

### Handoff Context
- Target branch: feat/chart-system/chart-foundation
- Single file: Chart.tsx with ChartContainer + context

<!-- LAT: 2026-03-11T03:49:48Z -->
<!-- PID: 77812 -->
<!-- auto-queue -->
<!-- target-branch: fix/theme-overhaul/hardcoded-value-audit -->
# Task: Replace hardcoded values in form components and primitives

Replace hardcoded hex colors and fallback values in form-related components and primitives.

## Target branch
`fix/theme-overhaul/hardcoded-value-audit`

## Files to modify (ONLY these files)
- `packages/components/Input/Input.tsx`
- `packages/components/Textarea/Textarea.tsx`
- `packages/components/Checkbox/Checkbox.tsx`
- `packages/components/Breadcrumb/Breadcrumb.tsx`
- `packages/components/Sidebar/Sidebar.tsx`
- `packages/components/Tooltip/Tooltip.tsx`
- `packages/primitives/Spinner.tsx`
- `packages/primitives/Label.tsx`

## CSS Variable Convention
Components use `var(--stl-{token}, {fallback})` pattern or STL `styled()` with `$token` syntax.

## What to replace

### Input.tsx
- `'var(--stl-color-error9, var(--red10, #ef4444))'` → `'var(--stl-color-error9)'` (remove nested fallback chain)
- Same pattern appears twice (border and label color for error state)

### Textarea.tsx
- `borderColor: 'var(--stl-error9, red)'` → `borderColor: 'var(--stl-color-error9)'`
- `style={error ? { color: 'var(--stl-error9, red)' } : undefined}` → `'var(--stl-color-error9)'`

### Checkbox.tsx
- `'var(--color10, var(--stl-color-primary9))'` → `'var(--stl-color-primary9)'` (simplify to single var)
- Appears twice (checked background and indeterminate background)

### Breadcrumb.tsx
- `gap: 4` → keep as-is (maps to space[0.5] = 4, it's fine as a number)
- Check for any hex colors

### Sidebar.tsx
- `margin: 8` → keep as-is
- Check for any hex colors or hardcoded values

### Tooltip.tsx
- `'opacity var(--stl-animation-fastDuration, 150ms) ease'` → `'opacity var(--stl-animation-fastDuration) ease'` (remove 150ms fallback)
- Check for any other hardcoded values

### Spinner.tsx
- `SIZE_MAP = { sm: 20, md: 20, lg: 28 }` → these are intentional component-specific sizes, keep as-is
- `DOT_SIZE_MAP = { sm: 4, md: 4, lg: 8 }` → keep as-is (spinner dot dimensions)
- Add a brief comment: `// Spinner-specific fixed dimensions`

### Label.tsx
- `color: 'var(--stl-color-error9, var(--red10, #ef4444))'` → `color: 'var(--stl-color-error9)'`

## Key principles
- **Simplify CSS var fallback chains** — remove nested `var()` fallbacks and hex values
- **Keep component-specific fixed dimensions** (Spinner sizes, Breadcrumb gaps)
- **Error color pattern** — standardize on `var(--stl-color-error9)` everywhere
- **Do NOT touch .native.tsx files or test files.**

## Acceptance criteria
- No remaining hex color literals in CSS var fallbacks
- Error color references standardized to `var(--stl-color-error9)`
- No nested `var()` chains (simplify to single var reference)
- Build succeeds: `npx tsc --noEmit`
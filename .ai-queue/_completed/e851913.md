<!-- LAT: 2026-03-10T04:08:20Z -->
<!-- PID: 28592 -->
<!-- auto-queue -->
<!-- target-branch: feat/theming-hardening/animation-tokens -->
# Task: Add prefers-reduced-motion support and verify focus ring tokens

## Goal
Add prefers-reduced-motion media query support via tokens and verify focus ring token consistency.

## Instructions

1. Read `packages/stl/src/config/scales/animation.ts` — understand current duration/easing/transition tokens
2. Read `packages/stl/src/config/scales/outline.ts` — understand focus ring tokens
3. Read `packages/stl/src/config/styles.css.ts` — understand how global styles apply

4. **Add prefers-reduced-motion support:**
   - In `styles.css.ts`, add a `@media (prefers-reduced-motion: reduce)` block that:
     - Sets all animation/transition durations to `0.01s` (not `0s` to avoid some browser bugs)
     - Disables keyframe animations
   - This should be a global style, not per-component
   - Use the existing `globalStyle` from vanilla-extract

5. **Verify focus ring tokens are consistent:**
   - Check that `outline.ts` provides `outlineWidth`, `outlineOffset`, `outlineColor` tokens
   - Verify they're accessible via `token.outline.*` in styles.css.ts
   - Document the standard focus pattern: 2px solid, 2px offset, theme-aware color

6. **Verify hover/press color shift tokens exist:**
   - Check if `backgroundHover`, `backgroundPress`, `borderHover` etc. tokens exist in the color or surface scales
   - If they're inline in components rather than token-based, document this gap (don't fix — that's E3)

## Key files
- `packages/stl/src/config/scales/animation.ts`
- `packages/stl/src/config/scales/outline.ts`
- `packages/stl/src/config/styles.css.ts`

<!-- LAT: 2026-03-10T20:16:55Z -->
<!-- PID: 96331 -->
<!-- auto-queue -->
<!-- target-branch: chore/foundation-cleanup/tamagui-removal -->
# Task: Config & Infrastructure Cleanup

Remove all Tamagui references from config/infrastructure files.

## Files to modify

### 1. Delete `config/tamagui.config.ts` and `config/tamagui.config.d.ts`
These are deprecated stubs. Delete both files entirely.

### 2. `jest.config.js`
Line 26 has: `'node_modules/(?!(tamagui|@tamagui|react-native|@react-native|react-native-web|@tiptap|invariant|scheduler)/)'`
Remove `tamagui|@tamagui|` from the pattern. Keep the rest.

### 3. `.gitignore`
Remove the `.tamagui/` entry (line 3).

### 4. Delete `.tamagui/` directory
```bash
rm -rf .tamagui/
```

### 5. Check `tsconfig.json` and all `tsconfig.*.json` files at root
Remove any tamagui-related paths or references.

### 6. Check root `package.json`
Remove any tamagui-related dependencies, devDependencies, or config fields. The `build` script should NOT reference tamagui.

## Acceptance criteria
- No file in config/, root tsconfig, jest.config, .gitignore, or package.json references tamagui
- Build scripts still work (don't break anything)

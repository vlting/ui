<!-- LAT: 2026-03-09T22:19:33Z -->
<!-- PID: 5792 -->
<!-- auto-queue -->
<!-- target-branch: docs/dark-mode-css-delivery/examples-docs -->
# Task: Verify docs site compatibility with data-color-mode

## Files to modify
- `apps/docs/components/providers.tsx`

## Context
The docs site uses `next-themes` with `attribute="class"` (applies `dark` class to `<html>`). STL now uses `data-color-mode` attribute. These are separate mechanisms and should not conflict. However, we need to verify and document any integration points.

## Instructions

### 1. Check `apps/docs/components/providers.tsx`

Read the file and verify:
- `next-themes` is configured with `attribute="class"` — this is fine, it won't conflict with `data-color-mode`
- If `StlProvider` is used anywhere in the docs site, it will set `data-color-mode` on `<html>` alongside the `dark` class from `next-themes`

### 2. Add a comment documenting the dual-system

In `apps/docs/components/providers.tsx`, add a comment above the `ThemeProvider` explaining the two systems:

```tsx
{/*
  Dark mode is managed by two complementary systems:
  - next-themes: sets `class="dark"` on <html> for Tailwind CSS
  - @vlting/stl: sets `data-color-mode="dark"` on <html> for STL CSS variables
  Both can coexist — they use different attributes and don't conflict.
*/}
```

### 3. Check if docs site imports StlProvider

Search for `StlProvider` usage in the docs site. If it's used in component demos, it will automatically set `data-color-mode` when rendered. This is fine — the attribute is harmless alongside `next-themes`.

If `StlProvider` is NOT used in the docs site at all, just add the comment from step 2 and note that no changes are needed.

## Acceptance criteria
- Verified no conflict between `next-themes` class toggle and `data-color-mode`
- Comment added to providers.tsx documenting the dual-system
- If StlProvider is used in docs, verified it works correctly

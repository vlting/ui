<!-- LAT: 2026-03-10T22:14:18Z -->
<!-- PID: 96331 -->
<!-- auto-queue -->
<!-- target-branch: feat/showcase-polish/api-coverage-polish -->
# Task: Expand showcase-web API surface coverage and polish demo cards

## Context
The showcase-web app has 11 top-level pages + 15 component detail pages. Need to verify and expand coverage of all @vlting/ui exports. The app already has pages for Styling & Tokens, Primitives, Components, Forms, Overlays, Data Display, Navigation, Hooks, Blocks, and Icons.

## Files to modify
- Pages in `apps/showcase-web/src/pages/` — update existing pages to cover more components
- `apps/showcase-web/src/App.tsx` or router file — add routes if new pages needed

## Implementation

1. **Audit current coverage**: Check which @vlting/ui exports are demonstrated. Read the current pages to understand what's already shown.

2. **Expand missing coverage**: Ensure each page covers ALL relevant components from its category. Key areas likely missing:
   - Charts page (may not exist — add if missing, using AreaChart, BarChart, LineChart, PieChart, RadarChart, RadialChart)
   - Utils page (cn, mergeRefs, composeEventHandlers — may not have demos)
   - All blocks (AuthBlock, SidebarBlock, DashboardBlock, etc.)

3. **Polish demo cards**: Each demo section should have:
   - Clear section title
   - Descriptive label
   - Multiple states/variants shown side by side where applicable
   - Consistent spacing between sections

4. **Dark mode**: Verify all demos look correct in dark mode. Components should use brand-aware colors.

5. **Build verification**: After changes, run `npx vite build` to ensure the build still passes.

## Acceptance
- Showcase-web demonstrates: styling tokens, all primitives, all components, all blocks, all charts, all hooks, utils, icons
- Demo cards are well-organized with clear labels
- `npx vite build` succeeds
- Commit with conventional commit message
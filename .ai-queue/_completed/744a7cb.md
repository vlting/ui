<!-- LAT: 2026-03-10T05:30:00Z -->
<!-- PID: 29583 -->
<!-- auto-queue -->
<!-- target-branch: feat/docs-overhaul/interactive-examples -->
# Task: Add interactive examples and copy-paste patterns to docs

## Goal
Add shadcn/ui style interactive examples with usage code, variant examples, and props tables.

## Instructions

1. Read the docs app structure:
   - `apps/docs/app/` — pages
   - `apps/docs/components/` — UI components
   - `apps/docs/lib/registry.ts` — component registry

2. For each major component page, add:
   - **Install command** — `npm install @vlting/ui` (or usage import)
   - **Usage code block** — basic example with syntax highlighting
   - **Variant examples** — show all variants side-by-side
   - **Props table** — list props with types, defaults, descriptions
   - **Copy button** on code blocks

3. Create a reusable `CodeBlock` component if one doesn't exist:
   ```tsx
   // apps/docs/components/code-block.tsx
   export function CodeBlock({ code, language, copyable }: {...}) {...}
   ```

4. Create a `PropsTable` component:
   ```tsx
   // apps/docs/components/props-table.tsx
   export function PropsTable({ props }: { props: PropDef[] }) {...}
   ```

5. Apply to at least these component pages:
   - Button, Input, Card, Dialog, Tabs, Select, Checkbox, Switch

6. Follow shadcn/ui patterns: clean, minimal, well-organized sections.

## Key files
- `apps/docs/app/components/` — component pages
- `apps/docs/components/` — shared components

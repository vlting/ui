<!-- auto-queue -->
<!-- target-branch: fix/quality-audit -->

# Stage 3, Segment 4: Audit Partial Coverage — NavigationMenu, Sidebar, Calendar, Carousel, DatePicker

## Context

These components were updated in Stage 2 to use native buttons with `focusVisibleStyle` on some elements, but may have other interactive elements still missing focus indicators. This segment audits each file for completeness.

**Epic branch:** `fix/quality-audit`
**Feature flag:** `quality_audit`
**Parent issue:** #3 (Epic), #6 (Stage 3)

## Scope

- `packages/components/NavigationMenu/NavigationMenu.tsx`
- `packages/components/Sidebar/Sidebar.tsx`
- `packages/components/Calendar/Calendar.tsx`
- `packages/components/Carousel/Carousel.tsx`
- `packages/components/DatePicker/DatePicker.tsx`
- `packages/components/DatePicker/DateRangePicker.tsx`

## Instructions

### Read First
- `packages/FRONTEND_QUALITY.contract.md` — §2 (Accessibility)
- Each file — identify ALL interactive elements

### Standard focusVisibleStyle Pattern

```tsx
focusVisibleStyle: {
  outlineWidth: 2,
  outlineOffset: 2,
  outlineColor: '$outlineColor',
  outlineStyle: 'solid',
}
```

For items inside containers, use `outlineOffset: -2`.

### Changes Required

For each file:
1. **Read the file completely**
2. **Identify every interactive element** — any element with click/press handlers, pointer cursor, or interactive ARIA roles
3. **Check if each has `focusVisibleStyle`** — either directly or via a styledHtml component that includes it
4. **Add `focusVisibleStyle` to any that lack it**

#### Specific Notes

- **Sidebar Rail** (`cursor="col-resize"`) — this IS interactive (resize handle). Add `focusVisibleStyle`. Also ensure it has `tabIndex={0}` and `role="separator"` or similar if it doesn't already have one.
- **NavigationMenu Link** — uses `styledHtml('a')` which already has `focusVisibleStyle` in its definition. Verify this is sufficient.
- **Calendar/DatePicker day cells and nav buttons** — already have `focusVisibleStyle` via styledHtml button components from Stage 2. Verify coverage is complete.
- **Carousel dots and nav buttons** — already have `focusVisibleStyle` via styledHtml button components from Stage 2. Verify coverage is complete.

Elements that DON'T need focusVisibleStyle:
- Overlay/backdrop elements (fixed position click-to-dismiss overlays)
- Container elements that aren't directly interactive

### Important Constraints

- Do NOT change visual appearance beyond adding focus indicators
- Do NOT fix token discipline issues — that's Stage 4
- Do NOT change HTML element types — that was Stage 2

## Verification

- [ ] `npx tsc --noEmit` — 0 errors
- [ ] `npx biome check` on modified files — no new errors
- [ ] Every interactive element in NavigationMenu has focusVisibleStyle
- [ ] Every interactive element in Sidebar has focusVisibleStyle (including Rail)
- [ ] Every interactive element in Calendar has focusVisibleStyle
- [ ] Every interactive element in Carousel has focusVisibleStyle
- [ ] Every interactive element in DatePicker has focusVisibleStyle
- [ ] Every interactive element in DateRangePicker has focusVisibleStyle

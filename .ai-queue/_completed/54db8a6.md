<!-- LAT: 2026-03-10T03:55:32Z -->
<!-- PID: 28592 -->
<!-- auto-queue -->
<!-- target-branch: feat/theming-hardening/token-audit -->
# Task: Ensure transition tokens exist as CSS custom properties

## Goal
Verify transition tokens are properly exposed as CSS custom properties and add any missing ones.

## Instructions

1. Read `packages/stl/src/config/scales/animation.ts` — understand existing duration/transition tokens
2. Read `packages/stl/src/config/styles.css.ts` — check how animation tokens are composed into CSS vars

3. Verify these transition tokens exist as CSS custom properties:
   - Duration: fast (~150-250ms), normal (~250-350ms), slow (~500-750ms)
   - Easing: default (ease), ease-in, ease-out, ease-in-out
   - Common transitions: background-color, border-color, box-shadow, opacity, transform, color

4. If any are missing, add them to `animation.ts` following the existing pattern:
   ```ts
   const newToken = { ...hash.var, value: "..." } as const
   ```

5. Ensure `styles.css.ts` exposes them in `token.animation.*` and `theme.animation.*`

6. Do NOT modify component files — this task is scale-level only

## Key files
- `packages/stl/src/config/scales/animation.ts`
- `packages/stl/src/config/styles.css.ts`

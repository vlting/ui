<\!-- LAT: 2026-03-09T15:05:48Z -->
<\!-- PID: 5917 -->
<!-- auto-queue -->
<!-- target-branch: feat/stl-components/simple-components -->
# Task: Migrate simple styled components batch 1 (Typography, Table, NativeSelect, ScrollArea, Direction, Loader)

These are the simplest components — minimal or no Tamagui imports. Several may already not use Tamagui.

## Migration pattern
Import `styled` from `'../stl-react/src/config'`. Use CSS properties with `$token` references. For components already using `styledHtml()`, convert to stl `styled("tag", css)`.

## Files to modify

### `packages/components/Typography/Typography.tsx`
- Currently uses `styledHtml()` (Tamagui's HTML helper)
- Replace with stl `styled("h1", css)`, `styled("p", css)`, etc.
- Keep all the semantic HTML elements (h1-h6, p, small, blockquote, code, ul, li)
- Remove any Tamagui imports

### `packages/components/Table/Table.tsx`
- Currently uses native HTML elements with inline styles
- May have zero Tamagui imports — verify and clean up any remaining
- If already clean, no changes needed

### `packages/components/NativeSelect/NativeSelect.tsx`
- Uses `styledHtml('select')`, `styledHtml('option')` (Tamagui)
- Replace with stl `styled("select", css)`, `styled("option", css)`
- Remove `as any` casts that were needed for styledHtml

### `packages/components/ScrollArea/ScrollArea.tsx`
- Uses `styled(View)` — replace with `styled("div", css)`
- Keep the dangerouslySetInnerHTML for webkit-scrollbar CSS
- Remove @ts-expect-error comments
- Keep compound pattern (ScrollArea.Viewport, etc.)

### `packages/components/Direction/Direction.tsx`
- Context provider only — may have no Tamagui imports
- Verify and clean up any remaining Tamagui references

### `packages/components/Loader/Loader.tsx`
- Uses Primitives.Spinner — likely no Tamagui imports
- Verify and clean up

## Acceptance criteria
- Zero Tamagui imports in all 6 files
- Zero @ts-expect-error comments
- All component APIs preserved
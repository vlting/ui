<\!-- LAT: 2026-03-09T14:47:07Z -->
<\!-- PID: 5917 -->
<!-- auto-queue -->
<!-- target-branch: feat/stl-primitives/text-visual-primitives -->
# Task: Migrate Heading primitive to stl

Replace Tamagui `styledHtml('h1'..'h6')` with stl `styled("h1".."h6", ...)` in `packages/primitives/Heading.tsx`.

## Token mapping (Tamagui → stl)
- `$heading` → `$heading` (fontFamily)
- `$color` → `$defaultHeading` (color)
- fontSize: `$10` → `$h1`, `$8` → `$h2`, `$6` → `$h3`, `$5` → `$h4`, `$4` → `$h5`, `$3` → `$h6`
- lineHeight: `$10` → `$heading`, `$8` → `$heading`, etc.
- fontWeight: `$5` → `$700`, `$4` → `$600`

## Files to modify
- `packages/primitives/Heading.tsx` — rewrite using stl `styled()`
- `packages/primitives/Heading.test.tsx` — remove `@ts-expect-error`, update imports

## Implementation
Keep the `level` prop + `LEVEL_MAP` pattern. Replace `styledHtml` with stl `styled()`:

```tsx
import { styled } from '../stl-react/src/config'
import React from 'react'

const baseStyles = {
  fontFamily: "$heading",
  color: "$defaultHeading",
  margin: "0",
}

const H1Frame = styled("h1", { ...baseStyles, fontSize: "$h1", lineHeight: "$heading", fontWeight: "$700" }, "H1")
const H2Frame = styled("h2", { ...baseStyles, fontSize: "$h2", lineHeight: "$heading", fontWeight: "$700" }, "H2")
const H3Frame = styled("h3", { ...baseStyles, fontSize: "$h3", lineHeight: "$heading", fontWeight: "$600" }, "H3")
const H4Frame = styled("h4", { ...baseStyles, fontSize: "$h4", lineHeight: "$heading", fontWeight: "$600" }, "H4")
const H5Frame = styled("h5", { ...baseStyles, fontSize: "$h5", lineHeight: "$heading", fontWeight: "$600" }, "H5")
const H6Frame = styled("h6", { ...baseStyles, fontSize: "$h6", lineHeight: "$heading", fontWeight: "$600" }, "H6")

type HeadingFrame = typeof H1Frame
const LEVEL_MAP: Record<number, HeadingFrame> = {
  1: H1Frame, 2: H2Frame, 3: H3Frame, 4: H4Frame, 5: H5Frame, 6: H6Frame,
}

export interface HeadingProps {
  level?: 1 | 2 | 3 | 4 | 5 | 6
  children?: React.ReactNode
  [key: string]: unknown
}

export const Heading = React.forwardRef<HTMLHeadingElement, HeadingProps>(
  function Heading({ level = 2, ...rest }, ref) {
    const Component = LEVEL_MAP[level] ?? LEVEL_MAP[2]
    return <Component ref={ref} {...rest} />
  },
)
```

NOTE: Check that `$defaultHeading` exists as a color token. If not, use available heading color token from stl. Also verify `$h1` through `$h6` exist as fontSize tokens (they should per the fontSize scale file).

## Acceptance criteria
- Zero `@ts-expect-error` or `as any` or `AnyFC` casts
- No tamagui or @tamagui/web imports
- Exports `Heading` and `HeadingProps`
- Supports level 1-6 with correct semantic HTML elements
- TypeScript compiles
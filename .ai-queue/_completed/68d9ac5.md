# Commit History
- 994f443 fix(primitives): replace hardcoded sizes with design tokens in Spinner and Icon
- 68d9ac5 Merge branch 'q/001' — token discipline for Spinner and Icon primitives

<!-- target-branch: fix/quality-audit -->

# Segment 1: Token Discipline — Primitives

Replace hardcoded numeric values with design tokens in primitive components.

## Scope
- `packages/primitives/Spinner.tsx`
- `packages/primitives/Icon.tsx`
- `packages/primitives/Divider.tsx`
- `packages/primitives/Skeleton.tsx`

## Summary
- Spinner: added SIZE_TOKEN and DOT_SIZE_TOKEN maps using design tokens ($1, $2, $0.5) for Tamagui width/height props; kept numeric maps for dot-position math calculations
- Icon: changed default size from 20 to '$1' token; widened IconFC and IconProps types to accept string tokens
- Divider.tsx: confirmed no changes needed (1px thickness values are intentional)
- Skeleton.tsx: confirmed no changes needed (borderRadius: 1000 is pill shape)
- tsc --noEmit passes; lint errors are all pre-existing

<!-- auto-queue -->
# Commit History
- `5ec4d3d` — fix(kitchen-sink): suppress Tamagui v2 RC type errors in BrandLayout
- `a5d9006` — Merge branch 'q/001'

# Fix TypeScript Errors in BrandLayout.tsx

## Context

After the `asChild` refactor of BrandLayout.tsx, there are 17 TypeScript errors. All are caused by the well-known **Tamagui v2 RC `GetFinalProps` bug** where `GetProps<>` resolves all token-string values as `undefined`. This means `children`, `asChild`, and various style props on `XStack`, `YStack`, `View`, and `Text` components show type errors.

## Scope

- `examples/kitchen-sink/src/layouts/BrandLayout.tsx`

## Instructions

Add `// @ts-expect-error Tamagui v2 RC` directives above each erroring JSX element. The errors are on these lines (line numbers may shift as directives are added — work top to bottom):

1. Line 196 — `YStack` children error
2. Line 216 — `XStack` with `asChild`
3. Line 231 — `Text` children error
4. Line 247 — `XStack` with `asChild`
5. Line 271 — `Text` children error
6. Line 337-338 — string assignment errors
7. Line 343 — `XStack` (banner) children error
8. Line 358 — `XStack` children error
9. Line 360 — `View` with `asChild`
10. Line 368 — `Text` children error
11. Line 374 — `Text` children error
12. Line 379 — `XStack` children error
13. Line 384 — `XStack` with `asChild`
14. Line 403 — `Text` children error
15. Line 416 — `XStack` with `asChild`
16. Line 432 — `Text` children error

**Pattern:** Place `{/* @ts-expect-error Tamagui v2 RC */}` as a JSX comment on the line before each erroring element. BUT NOTE: JSX comments are NOT real TS directives. Instead, use `// @ts-expect-error Tamagui v2 RC` as a line comment ABOVE the JSX line. In TSX, this works when placed directly above the opening `<Tag`.

For the string assignment errors on 337-338, these are likely prop type issues — use `// @ts-expect-error` above the prop lines.

## Verification

1. Run `npx tsc --noEmit` from `examples/kitchen-sink/` — should produce 0 errors for BrandLayout.tsx
2. Remaining errors in other files (Section.tsx etc.) are pre-existing and out of scope

## Post-completion

- Commit with message: `fix(kitchen-sink): suppress Tamagui v2 RC type errors in BrandLayout`
- Push to `fix/quality-audit`

## Task Progress
<!-- lat: 2026-02-26T07:55:00Z -->
<!-- agent-pid: 52795 -->
<!-- worktree: .worktrees/q-001 -->
<!-- branch: q/001 -->

### Checklist
- [ ] **ACTIVE** → Create worktree from fix/quality-audit
- [ ] Add AnyFC casts for Tamagui components to suppress GetFinalProps bug
- [ ] Verify tsc --noEmit passes for BrandLayout.tsx
- [ ] Commit and push to fix/quality-audit

### Handoff Context
- All 17 errors are the standard Tamagui v2 RC GetFinalProps bug
- Plan: use AnyFC cast pattern (ComponentType<Record<string, unknown>>) at top of file

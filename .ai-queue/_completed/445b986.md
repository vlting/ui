<!-- LAT: 2026-03-09T15:05:35Z -->
<!-- PID: 5792 -->
<!-- auto-queue -->
<!-- target-branch: feat/stl-components/simple-components -->
# Task: Migrate Alert, Card, Avatar to stl styled()

Migrate these 3 priority components from Tamagui to stl.

## Migration pattern
Import `styled` from `'../stl-react/src/config'`. Use CSS properties with `$token` references. For compound components, use `withStaticProperties` pattern or simple object assignment (e.g., `Alert.Title = AlertTitle`).

See `packages/primitives/Badge.tsx` and `packages/components/Alert/Alert.tsx` for reference patterns.

## Files to modify

### `packages/components/Alert/Alert.tsx`
- Replace all `styled(XStack)`, `styled(Text)`, `styled(View)` with `styled("div", {...})`, `styled("span", {...})` etc.
- Remove all Tamagui imports
- Remove all @ts-expect-error comments
- Keep compound pattern (Alert.Title, Alert.Description, Alert.Icon)
- Map Tamagui props to CSS: `gap: '$2'` stays, `backgroundColor` stays, `borderRadius` stays
- Use `"section"` for root, `"span"` for text elements, `"div"` for containers

### `packages/components/Card/Card.tsx`
- Replace all `styled(TamaguiCardFrame)`, `styled(YStack)`, `styled(Text)` with stl styled
- Remove `withStaticProperties` if present — use simple `Card.Header = CardHeader` pattern
- Remove all @ts-expect-error comments (16 of them)
- Map all token references: `$color`, `$borderColor`, `$radius.md`, etc.
- Use `"article"` for Card root, `"header"` for Header, `"div"` for Content/Footer, `"h3"` for Title, `"p"` for Description

### `packages/components/Avatar/Avatar.tsx`
- Replace styled(View), styled(Text), Image with stl styled equivalents
- Keep the context/state logic (createContext, useContext, useState for image error handling)
- Remove all @ts-expect-error comments and AnyFC casts
- Use `"div"` for container, `"img"` for image, `"span"` for fallback text
- The Avatar is behavioral — keep hooks, just replace styling primitives

## Acceptance criteria
- Zero Tamagui imports in all 3 files
- Zero @ts-expect-error comments
- Compound sub-component pattern preserved
- All existing props/variants maintained

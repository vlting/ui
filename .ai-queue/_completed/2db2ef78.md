<!-- LAT: 2026-03-10T21:50:05Z -->
<!-- PID: 77812 -->
<!-- auto-queue -->
<!-- target-branch: feat/docs-polish/complete-docs-deploy -->
# Task: Add Vercel deployment config for docs site

## Context
The docs site (`apps/docs/`) is a Next.js 15 app that needs Vercel deployment configuration for docs.vlt.ing.

## Files to create
- `apps/docs/vercel.json` — Vercel project configuration (NEW)

## Implementation

1. **Create `apps/docs/vercel.json`**:
   ```json
   {
     "$schema": "https://openapi.vercel.sh/vercel.json",
     "framework": "nextjs",
     "installCommand": "cd ../.. && yarn install --frozen-lockfile",
     "buildCommand": "cd ../.. && yarn build:docs",
     "outputDirectory": ".next"
   }
   ```

   Key considerations:
   - The docs app is in a monorepo at `apps/docs/`
   - It imports from `../../packages/*` (relative paths, not published packages)
   - Vercel needs to install from root to get all dependencies
   - Build command should run from root to ensure proper workspace resolution
   - Output is standard Next.js `.next` directory

2. **Verify the build works**: The `yarn build:docs` script runs `yarn workspace @vlting/docs build`. This should work since the docs workspace depends on root packages.

3. **Do NOT create a Vercel project** (that requires the Vercel CLI with auth). Just create the config file.

## Acceptance
- `apps/docs/vercel.json` exists with correct configuration
- Config points to correct build command and output directory
- Install command handles monorepo dependency resolution
- Commit with conventional commit message
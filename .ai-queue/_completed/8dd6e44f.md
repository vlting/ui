<!-- LAT: 2026-03-10T21:47:21Z -->
<!-- PID: 96331 -->
<!-- auto-queue -->
<!-- target-branch: feat/docs-polish/complete-docs-deploy -->
# Task: Create hooks documentation pages

## Context
The docs site has no hooks section. There are 8 hooks in `packages/hooks/` that need documentation pages. The docs site uses Next.js with dynamic routing.

## Available hooks (in `packages/hooks/`)
1. `useClipboard` — clipboard copy with status feedback
2. `useControllableState` — controlled/uncontrolled state pattern
3. `useDebounce` — debounce value changes
4. `useFocusTrap` — trap focus within a container
5. `useIntersectionObserver` — observe element visibility
6. `useKeyboardNavigation` — arrow key navigation for lists
7. `useMediaQuery` — responsive media query matching
8. `useReducedMotion` — detect prefers-reduced-motion

## Files to create/modify
- `apps/docs/app/docs/hooks/page.tsx` — hooks index page (NEW)
- `apps/docs/app/docs/hooks/[name]/page.tsx` — individual hook page (NEW)
- `apps/docs/lib/hook-registry.ts` — hook registry (NEW)
- `apps/docs/lib/navigation.ts` — add hooks section

## Implementation

1. **Create `apps/docs/lib/hook-registry.ts`**: Registry similar to component registry. Each entry:
   ```ts
   { name: 'useClipboard', slug: 'use-clipboard', description: '...', usage: '...code...' }
   ```
   Read each hook file in `packages/hooks/` to extract the function signature and write a concise usage example.

2. **Create `apps/docs/app/docs/hooks/page.tsx`**: Index page listing all hooks with links. Follow the pattern of other section pages (e.g., blocks index if it exists, or components).

3. **Create `apps/docs/app/docs/hooks/[name]/page.tsx`**: Dynamic page for individual hooks. Show:
   - Hook name and description
   - Import statement with Shiki highlighting
   - Usage example with Shiki highlighting
   - Parameters / return value documentation
   - Use `generateStaticParams()` for static generation

4. **Update `apps/docs/lib/navigation.ts`**: Add a "Hooks" section with all 8 hooks listed.

## Acceptance
- All 8 hooks have documentation pages accessible via the docs navigation
- Each hook page shows: import statement, usage example, parameter descriptions
- Hooks section appears in sidebar navigation
- `yarn build:docs` succeeds with new hook pages
- Commit with conventional commit message
# Commit History
- `971a22c` fix(table): use native HTML elements for proper table display context

---

<!-- auto-queue -->
# Fix Table Component — Proper HTML Table Rendering

## Result
- Replaced Tamagui `styled(View, { tag: 'table' })` approach with thin React wrappers around native HTML elements (Approach C)
- Each sub-component (`Root`, `Header`, `Body`, `Footer`, `Row`, `Head`, `Cell`, `Caption`) wraps the corresponding native element
- Styling uses CSS custom properties (`var(--f-body)`, `var(--borderColor)`, etc.) that map to Tamagui theme tokens at runtime
- Header cells get `backgroundColor: var(--color2)`, proper `fontWeight: 600`, `textAlign: left`
- Caption uses `captionSide: bottom` for clean shadcn/ui-style layout
- Updated kitchen-sink TableDemo with 4-column table (Name, Role, Status, Hours), 5 data rows, footer total, and caption
- Build verified clean (963 modules, 497KB gzip 157KB)

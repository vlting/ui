<!-- LAT: 2026-03-09T05:34:40Z -->
<!-- PID: 54261 -->
<!-- auto-queue -->
<!-- target-branch: chore/stl-foundation/package-scaffold-rebrand -->
# Task: Copy & rebrand @vlting/stl (core)

## What
Copy quarks core from neutron-ui into `packages/stl/` and rebrand all references.

## Steps

1. **Copy source files:**
   ```
   cp -R /Users/lucas/Sites/neutron-ui/packages/quarks/src/ packages/stl/src/
   cp /Users/lucas/Sites/neutron-ui/packages/quarks/package.json packages/stl/
   cp /Users/lucas/Sites/neutron-ui/packages/quarks/tsconfig.json packages/stl/
   cp /Users/lucas/Sites/neutron-ui/packages/quarks/vite.config.ts packages/stl/
   ```

2. **Rebrand package.json:**
   - `name`: `@withneutron/quarks` → `@vlting/stl`
   - `description`: Update to mention "STL - Style Token Layer"
   - Output filenames: `quarks.*` → `stl.*` (main, module, exports)
   - Remove neutron-specific metadata (author URLs, bugs pointing to neutron-ui)
   - Set bugs/repo to `vlting/ui`

3. **Rebrand vite.config.ts:**
   - Output filenames: `quarks.js` → `stl.js`, `quarks.es.js` → `stl.es.js`, `quarks.css` → `stl.css`

4. **Production-harden StyleManager.ts:**
   - Remove ALL `console.log` statements (search for `console.log` in the file)

5. **Production-harden styles.css.ts:**
   - Remove ALL `console.log` statements

6. **Search ALL files in `packages/stl/src/`** for remaining references to:
   - `@withneutron` → replace with `@vlting`
   - `quarks` (lowercase, in strings/identifiers) → replace with `stl`
   - `Quarks` (PascalCase) → replace with `Stl`
   - `QUARKS` (uppercase) → replace with `STL`
   - `neutron` → remove or replace with `vlt`

7. **Verify:** Run `grep -r "quarks\|Quarks\|QUARKS\|withneutron\|neutron" packages/stl/` to confirm zero remaining references.

## Files touched
- `packages/stl/**` (new files only, no existing file conflicts)

## Acceptance criteria
- All quarks core source is in `packages/stl/src/`
- Zero references to quarks/withneutron/neutron
- No debug console.log in StyleManager or styles.css.ts
- package.json has correct `@vlting/stl` name and build outputs

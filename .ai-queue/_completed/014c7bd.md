<!-- auto-queue -->
# Commit History
- `0a87867` feat(design-tokens): add outline theme values and purple-tint fun palette
- `014c7bd` feat(design-tokens): outline theme values and fun purple palette (#001)

# 001 — Design Tokens: Focus Outline System & Fun Theme Purple Palette

## Summary

Update the design token layer to:
1. Expose outline width/offset as theme-level values that components can reference
2. Change the default outline offset from 2 to 1
3. Redesign the fun brand as a monochromatic purple palette with purple-tinted neutrals

## Scope

- `packages/design-tokens/brands/index.ts`
- `packages/design-tokens/brands/fun.ts`
- `packages/design-tokens/themes.ts`
- `packages/design-tokens/base.ts`

## Acceptance Criteria

- [x] `$outlineWidth` and `$outlineOffset` are available as theme values in styled components
- [x] Default outline offset is 1px (not 2px)
- [x] Fun brand neutrals are purple-tinted (faint purple undertone, not warm amber)
- [x] Fun brand accent palette remains saturated purple
- [x] All existing brands still build and theme correctly
- [x] No TypeScript errors in the design-tokens package

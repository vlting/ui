<!-- LAT: 2026-03-15T18:37:00Z -->
<!-- PID: $PPID -->
<!-- auto-queue -->
<!-- target-branch: chore/library-buildout/button-spec-test-audit -->
# Task: Audit Button.spec.md

Update `packages/components/Button/Button.spec.md` to match the actual implemented API.

## Checklist
- [ ] Read Button.tsx and Button.native.tsx for implementation truth
- [ ] Read current Button.spec.md
- [ ] Update Section 3 (Anatomy)
- [ ] Update Section 6 (Styling / Token Names)
- [ ] Add Section 10 (Cross-Platform)
- [ ] Ensure default variants documented
- [ ] Commit changes

## Changes Required

### Section 3 (Anatomy)
- Remove reference to `ButtonFrame` wrapper — implementation is `styled('button', opts)` directly
- Document internal sub-elements: `ButtonSpinner` (absolute-positioned span), `ButtonContent` (display:contents span)
- Note that `styled()` handles forwardRef implicitly

### Section 6 (Styling / Token Names)
- Fix token references: neutral theme uses `$tertiary*` tokens (not `$neutral*`)
- Fix token references: destructive theme uses `$error*` tokens (not `$red*`)
- Document the actual compound variant step mapping:
  - solid: `$color9`/`$colorText9` idle, `$color10`/`$colorText10` interact
  - subtle: `$color3`/`$colorText3` idle, `$color4`/`$colorText4` interact
  - outline: transparent bg + `$color7` border, `$color3` interact
  - ghost: transparent bg, `$color3` interact
  - link: transparent bg, underline, `$color9` text

### Section 10 (NEW: Cross-Platform)
Add a new section documenting web vs native divergences:
- Web uses `onClick`, native uses `onPress`
- Web uses `template` with `prefix`/`suffix` slots, native uses `Button.Text`/`Button.Icon` sub-components
- Web has 4×5=20 compound variants (theme×variant), native has 7 collapsed variants
- Web `styled()` has `compoundVariants`/`mapProps`/`template`, native `styled()` does not
- Document this as intentional platform divergence, not a bug

### Default Variants
Ensure the spec explicitly states default variant values:
```
defaultVariants: { theme: 'primary', variant: 'solid', size: 'md' }
```

### Reference Files
- Read `packages/components/Button/Button.tsx` for implementation truth
- Read `packages/components/Button/Button.native.tsx` for native divergences
- Do NOT modify any implementation files — spec changes only

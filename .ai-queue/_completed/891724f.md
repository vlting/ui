# Commit History
- `9dc9d6f` — fix(docs): use .Root for plain object compound components
- `891724f` — merge: fix(docs): build verification and compound component fixes (004)

<!-- auto-queue -->
<!-- depends-on: 001, 002, 003 -->
<!-- target-branch: feat/docs-site/component-pages -->
# Integration — Build Verification + Fixes

Integration segment that verifies all Stage 2 pieces work together and fixes any issues.

## Context
- Segment 001 built: registry.ts, api-mapping.ts
- Segment 002 built: code-block.tsx, prop-table.tsx, component-preview.tsx, added shiki
- Segment 003 built: [name]/page.tsx, component-renderer.tsx, playground.tsx
- All segments target `feat/docs-site/component-pages` branch

## Instructions

### 1. Verify the build

```bash
cd examples/docs
npx next build
```

If there are TypeScript errors or build failures, fix them. Common issues to watch for:
- Import path mismatches between segments
- Missing type exports
- shiki API changes (v3 has different API than v1)
- Server/client component boundary issues (server components can't use hooks, client components need 'use client')
- `generateStaticParams` returning incorrect format

### 2. Verify all 10 exemplar pages build

Check that all 10 exemplar component pages generate correctly:
- `/docs/components/button`
- `/docs/components/input`
- `/docs/components/card`
- `/docs/components/dialog`
- `/docs/components/tabs`
- `/docs/components/select`
- `/docs/components/accordion`
- `/docs/components/table`
- `/docs/components/form`
- `/docs/components/tooltip`

### 3. Verify non-exemplar pages build

Check that a few non-exemplar pages also generate (they should show the basic template without live examples):
- `/docs/components/alert`
- `/docs/components/checkbox`
- `/docs/components/slider`

### 4. Run lint

```bash
cd /path/to/repo/root
yarn lint
```

Fix any lint issues in the `examples/docs/` directory.

### 5. Fix any issues found

Address all build, type, or lint issues. Ensure the docs site builds cleanly.

## Scope
- Any files in `examples/docs/` that need fixes
- `examples/docs/package.json` (if dependency adjustments needed)

## Verification
- `npx next build` in `examples/docs/` completes successfully
- All component pages listed in navigation generate as static pages
- `yarn lint` passes (or only has pre-existing warnings)
- No TypeScript compilation errors

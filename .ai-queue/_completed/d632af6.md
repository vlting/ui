# Commit History
- `3faf4d4` — chore(docs): relocate docs site from examples/docs to apps/docs
- `d632af6` — merge commit to chore/docs-site/relocation

<!-- auto-queue -->
<!-- target-branch: chore/docs-site/relocation -->
# Move docs site from examples/docs/ to apps/docs/

## Instructions

### Step 1: Move the directory
```bash
mkdir -p apps
git mv examples/docs apps/docs
```

### Step 2: Update root package.json
- Add `"apps/*"` to the `workspaces` array (keep `"examples/*"` since other examples still exist)
- Update `dev:docs` script from `yarn workspace @vlting/examples-docs dev` to `yarn workspace @vlting/docs dev`

### Step 3: Update apps/docs/package.json
- Change `"name"` from `"@vlting/examples-docs"` to `"@vlting/docs"`

### Step 4: Update root tsconfig.json
- Add `"apps"` to the `exclude` array (alongside existing `"examples"`)

### Step 5: Verify relative imports are still correct
Since `examples/docs/` and `apps/docs/` are at the same depth (2 levels from root), relative imports like `../../packages/` and `../../../packages/` should NOT change. Verify a few key files to confirm:
- `apps/docs/components/block-preview.tsx` — should still have `../../../packages/blocks/`
- `apps/docs/app/docs/charts/chart-preview.tsx` — should still have `../../../../../packages/components/Chart`
- `apps/docs/tsconfig.json` — paths like `../../packages/*` should still be correct

If any relative paths are wrong, fix them.

### Step 6: Test the build
Run `yarn install` (to update workspace links) and then try `yarn dev:docs` to verify it works.

## Scope
- `apps/docs/**` (all files moved from examples/docs/)
- `package.json` (root — workspaces + scripts)
- `tsconfig.json` (root — exclude)
- `apps/docs/package.json` (name change)

## Verification
- [ ] `apps/docs/` exists with all files
- [ ] `examples/docs/` no longer exists
- [ ] Root package.json has `apps/*` in workspaces
- [ ] Root package.json dev:docs references new workspace name
- [ ] apps/docs/package.json has updated name
- [ ] Root tsconfig.json excludes `apps`
- [ ] `yarn install` succeeds
- [ ] `yarn dev:docs` starts without errors

## Task Progress
<!-- lat: 2026-03-02T20:46:25Z -->
<!-- agent-pid: 68095 -->
<!-- worktree: .worktrees/q-001 -->
<!-- branch: q/001 -->

### Checklist
- [ ] **ACTIVE** → Create worktree and move directory
- [ ] Update root package.json (workspaces + scripts)
- [ ] Update apps/docs/package.json (name)
- [ ] Update root tsconfig.json (exclude)
- [ ] Verify relative imports
- [ ] Test build (yarn install + yarn dev:docs)
- [ ] Commit, rebase, merge, archive

### Handoff Context
- Target branch: chore/docs-site/relocation
- Both paths same depth (2 levels from root), relative imports should NOT change

<!-- auto-queue -->
<!-- depends-on: 014 -->
<!-- target-branch: feat/library-buildout/content-components -->

# Task: Redesign ItemSection playground — media/actions toggles + horizontal theme row

## Files
- `apps/playground/src/sections/ItemSection.tsx`

## Instructions

1. **Update variant names**: Change `ITEM_VARIANTS` from `['default', 'subtle', 'outline']` to `['ghost', 'subtle', 'outline']`.

2. **Set default variant to `'outline'`** in useState: `useState<...>('outline')`.

3. **Add boolean toggles for media and actions**:
   - `const [showMedia, setShowMedia] = useState(true)`
   - `const [showActions, setShowActions] = useState(true)`
   - Add toggle Buttons in the ButtonRow for "media" and "actions"
   - Conditionally render `<Item.Leading>` and `<Item.Trailing>` based on these toggles

4. **Horizontal row layout for theme groups**:
   - Replace the vertical `StackY` wrapping the theme groups with a horizontal flex container
   - Use `display: 'flex'`, `flexWrap: 'wrap'`, `gap: '$16'`
   - Each theme group should have `flex: '1'`, `minWidth: '280px'` so they sit side-by-side on wide screens and wrap to multi-row on narrow screens

5. **Keep existing toggle controls** (variant, size, interactive) working as before.

## Checklist
- [ ] Update ITEM_VARIANTS to use 'ghost'
- [ ] Default variant useState to 'outline'
- [ ] Add showMedia toggle + Button
- [ ] Add showActions toggle + Button
- [ ] Conditionally render Leading/Trailing
- [ ] Horizontal flex-wrap layout for theme groups
- [ ] Verify playground renders correctly

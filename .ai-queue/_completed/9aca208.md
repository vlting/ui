# Commit History
- f613b11 — fix(tokens): replace hardcoded pixel values with design tokens in Avatar, Pagination, Resizable
- 9aca208 — Merge branch 'q/005' — token discipline for navigation & data components

<!-- target-branch: fix/quality-audit -->

# Segment 5: Token Discipline — Navigation & Data Display

Replace hardcoded numeric values with design tokens in navigation and data display components.

## Scope
- `packages/components/Breadcrumb/Breadcrumb.tsx`
- `packages/components/Pagination/Pagination.tsx`
- `packages/components/Avatar/Avatar.tsx`
- `packages/components/Table/Table.tsx`
- `packages/components/Resizable/Resizable.tsx`
- `packages/components/Accordion/Accordion.tsx`

## Instructions

Read `AI_CONSTITUTION.md` and `DESIGN_CONSTITUTION.md` before starting.

### Token Reference

**Size tokens** (width, height): $0.25=2, $0.5=4, $0.75=8, $1=20, $1.5=24, $2=28, $2.5=32, $3=36, $3.5=40, $4=44, $4.5=48
**Space tokens** (padding, margin, gap): $0.25=2, $0.5=4, $0.75=8, $1=10, $1.5=12, $2=16, $2.5=18, $3=20, $3.5=24, $4=28, $4.5=32
**Radius tokens** (borderRadius): $0=0, $1=3, $2=5, $3=7, $4=9, $5=10, $6=16
**Font size tokens**: $1=11, $2=12, $3=13, $4=14, $5=16, $6=18, $7=20, $8=24

### Breadcrumb.tsx
- `gap: 4` (appears twice, lines ~60, ~70) → `gap: '$0.5'`
- `borderRadius: 2` → keep as literal (no exact radius token; `$1`=3 is close but not exact)
- CSS string `outline: 2px` / `outline-offset: 1px` / `border-radius: 2px` → keep in CSS string (these are inside a template literal for focusVisibleStyle, not Tamagui props)

### Pagination.tsx
- `PAGE_BUTTON_MIN_WIDTH`: `sm: 28` → `'$2'` (size token), `md: 36` → `'$3'`, `lg: 44` → `'$4'`
- Note: Map values that become tokens are strings. Ensure types accommodate `string | number`.

### Avatar.tsx
- Size variants: `sm: { width: 32, height: 32 }` → `{ width: '$2.5', height: '$2.5' }`
- `md: { width: 40, height: 40 }` → `{ width: '$3.5', height: '$3.5' }`
- `lg: { width: 56, height: 56 }` → keep literal (no size token at 56)
- `xl: { width: 72, height: 72 }` → keep literal (no size token at 72)
- `borderRadius: 1000` → keep (circle shape)
- `SIZE_PX` map (if used for non-Tamagui purposes like image `src` sizing) → keep as numeric literals since these may be passed to external APIs

### Table.tsx
- `fontSize: 'var(--f-size-3, 14px)'` → `fontSize: '$4'` (Tamagui font token, replaces CSS var reference)
- `fontSize: 'var(--f-size-2, 12px)'` → `fontSize: '$2'`
- `padding: '8px 12px'` → convert to `paddingVertical: '$0.75'` and `paddingHorizontal: '$1.5'` (Tamagui individual properties)
- `padding: '8px 0'` → convert to `paddingVertical: '$0.75'` and `paddingHorizontal: 0`
- Note: Table uses CSS string values in styled() config. Convert to Tamagui property syntax.

### Resizable.tsx
- Handle dimension `4` (width or height depending on orientation) → `'$0.5'` (size token)
- Indicator dimensions: `width: 4` / `height: 16` → `4` → `'$0.5'`, `16` → keep literal (no size token at 16)
- `Math.max(5, ...)` (minimum panel size constraint) → keep as literal (this is a layout math constraint, not a styled prop)
- `borderRadius: 9999` → keep (pill shape)

### Accordion.tsx
- `borderRadius: 1000` → keep (circle shape)
- `borderBottomWidth: 1` → keep as literal (border thickness)
- **Result: Likely no token changes** — review for any missed spacing/gap values

## Rules
1. Use exact token matches ONLY. Keep literals where no exact token exists.
2. Do NOT change opacity, flex, zIndex, or zero values.
3. Do NOT change focusVisibleStyle outlineWidth/outlineOffset.
4. Do NOT change borderRadius 999/1000/9999 (pill shapes).
5. Preserve visual behavior — pure token substitutions only.
6. When map values change from numbers to strings, update TypeScript types accordingly.
7. For Table.tsx CSS string values, convert to individual Tamagui properties with tokens.

## Verification
- `npx tsc --noEmit` passes
- `yarn lint` passes
- No visual regressions in Avatar sizes, Table cell spacing, Pagination button sizes, or Breadcrumb layout

## Task Progress
<!-- lat: 2026-02-26T08:13:42Z -->
<!-- agent-pid: 52795 -->
<!-- worktree: .worktrees/q-005 -->
<!-- branch: q/005 -->

### Checklist
- [ ] **ACTIVE** → Create worktree from fix/quality-audit
- [ ] Tokenize Breadcrumb.tsx
- [ ] Tokenize Pagination.tsx
- [ ] Tokenize Avatar.tsx
- [ ] Tokenize Table.tsx
- [ ] Tokenize Resizable.tsx
- [ ] Review Accordion.tsx (likely no changes)
- [ ] Verify tsc --noEmit passes
- [ ] Commit, rebase, merge, archive

### Handoff Context
- Target branch: fix/quality-audit
- 6 files in scope, Accordion likely no changes needed

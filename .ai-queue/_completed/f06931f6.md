<!-- auto-queue -->
<!-- target-branch: feat/library-buildout/content-components -->

# Task: Replace inherit with explicit size variants on ItemTitle

## Context

Previous commit used `fontSize: 'inherit'` on ItemTitle + `fontSize` on ItemRoot size variants. `inherit` is CSS-only — won't work on React Native. Fix: move fontSize to explicit size variants on ItemTitle, remove from ItemRoot.

## Changes

**File:** `packages/components/Item/Item.tsx`

1. **ItemRoot size variants** — remove `fontSize` (revert to layout-only):
   ```ts
   sm: { minHeight: '$36', py: '$4', px: '$8' },
   md: { minHeight: '$44', py: '$8', px: '$12' },
   lg: { minHeight: '$52', py: '$12', px: '$16' },
   ```

2. **ItemTitle** — replace `fontSize: 'inherit'` with size variants:
   ```ts
   const ItemTitle = styled('span', {
     fontWeight: '$600',
     fontSize: '$14',
     lineHeight: '$listItem',
     color: 'inherit',
   }, {
     name: 'ItemTitle',
     variants: {
       size: {
         sm: { fontSize: '$13' },
         md: { fontSize: '$14' },
         lg: { fontSize: '$p' },
       },
     },
     defaultVariants: { size: 'md' },
   })
   ```

3. Update `ItemTitleProps` export if needed to reflect new variant type.

## Checklist

- [ ] Remove fontSize from ItemRoot size variants
- [ ] Add size variants to ItemTitle with explicit tokens
- [ ] Verify build passes

<!-- LAT: 2026-03-10T04:01:51Z -->
<!-- PID: 29583 -->
<!-- auto-queue -->
<!-- target-branch: feat/theming-hardening/brand-customization -->
# Task: Add multi-brand demo to showcase-web

## Goal
Add a brand switcher and multi-brand demo to the showcase-web app.

## Instructions

1. Read existing brand definitions:
   - `packages/design-tokens/brands/default.ts`
   - `packages/design-tokens/brands/fun.ts`
   - `packages/design-tokens/brands/posh.ts`
   - `packages/design-tokens/brands/shadcn.ts`

2. Read how docs app does brand switching:
   - `apps/docs/components/providers.tsx` — `useBrand()` hook pattern
   - `apps/docs/components/brand-switcher.tsx` — UI component

3. Read showcase-web app structure:
   - `apps/showcase-web/src/App.tsx`
   - `apps/showcase-web/src/main.tsx`

4. Add a brand switcher to the showcase sidebar:
   - Import `{ defaultBrand, funBrand, poshBrand, shadcnBrand }` from `@vlting/ui/design-tokens`
   - Import `{ injectBrandVars }` from `@vlting/ui/design-tokens`
   - Create a simple dropdown/select in the sidebar that switches brands
   - On brand change, inject CSS variables via `:root` style updates
   - Persist selection to localStorage

5. The brand switch should work with both light and dark modes (use the current color mode to pick the right brand palette).

6. Test: switching brands should visually update ALL components on the current page — colors, spacing, radius, shadows should all change.

## Key files
- `apps/showcase-web/src/App.tsx` (or sidebar component)
- `apps/showcase-web/src/main.tsx`
- `packages/design-tokens/brands/`

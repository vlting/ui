# Commit History
- 1f13f6c fix(components): replace hardcoded sizes with design tokens in menu components
- ab13128 Merge branch 'q/001' — token discipline for menu & context components

<!-- auto-queue -->
<!-- target-branch: fix/quality-audit -->

# Fix: Token Discipline — Menu & Context Components

## Summary
- Command: fontSize ($4, $2), padding ($0.5, $1.5, $3.5), margin ($0.5, $0.75) to tokens
- DropdownMenu: padding ($0.5), margin ($0.5, $0.25), fontSize ($4, $2), marginLeft ($3.5) to tokens
- ContextMenu: same pattern as DropdownMenu
- Menubar: padding ($0.5), gap ($0.25), margin ($0.5), fontSize ($4, $2), marginLeft ($3.5) to tokens
- Negative margins (marginLeft/Right={-4}) kept as literals (no negative token in reference)
- paddingTop={6} kept as literal (no exact token match)

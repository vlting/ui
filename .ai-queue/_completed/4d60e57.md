# Commit History

- `b168ed3` — feat(specs): create specs for 8 undocumented primitives
- `4d60e57` — feat(specs): create specs for 8 undocumented primitives (#010) [merge]

---

<!-- auto-queue -->
<!-- depends-on: 007 -->
# Create Specs for Missing Primitives

All 8 primitives were fully implemented (no stubs needed). Specs created as flat files at `packages/primitives/` alongside the source files.

## Primitives Documented (8)
1. AspectRatio — plain React, padding-bottom technique, web-only
2. Badge — styled(Text), 5 variants + 3 sizes, shadcn naming
3. Kbd — styledHtml('kbd'), semantic keyboard input element
4. Label — wraps @tamagui/label, htmlFor association, required indicator
5. Separator — styled(View), role="separator", aria-orientation, decorative mode
6. Skeleton — styled(View), lazy animation, circle variant, aria-hidden
7. Spinner — 8-dot rotation, role="status", aria-label, 3 sizes
8. VisuallyHidden — sr-only technique, accessible but visually hidden

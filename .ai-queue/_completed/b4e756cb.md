<!-- LAT: 2026-03-18T18:25:00Z -->
<!-- PID: $PPID -->
<!-- auto-queue -->
<!-- target-branch: feat/library-buildout/content-components -->

# Task: Badge section — variant toggle buttons

## Files
- `apps/playground/src/sections/BadgeSection.tsx`

## Checklist

- [ ] Add `useState<'solid' | 'subtle' | 'outline'>('solid')` for active variant
- [ ] Replace the three static variant sections (Solid/Subtle/Outline) with a single set of rows that renders badges using the active variant state
- [ ] Add a `ButtonRow` of three `Button` toggle controls (Solid / Subtle / Outline) above the badge rows — use `variant={active === x ? 'solid' : 'ghost'}` pattern for active state
- [ ] Keep the Sizes section as-is at the bottom

## Constraints
- Import `Button` from `@vlting/ui` alongside `Badge`
- All styling via STL — no inline `style={}`
- Use existing `ButtonRow`, `SectionTitle`, `StackY` from shared
- Button toggle row: `theme="neutral"` `size="sm"` on each toggle button

<!-- LAT: 2026-03-10T21:30:17Z -->
<!-- PID: 96331 -->
<!-- auto-queue -->
<!-- target-branch: fix/docs-polish/component-rendering -->
# Task: Fix block and chart preview rendering

## Context
Block previews (`block-preview.tsx`) and chart previews (`chart-preview.tsx`) render components without StlProvider context, resulting in unstyled output. After Task 001 adds StlProvider to root providers, these should inherit styling. But they may need additional fixes.

## Files to modify
- `apps/docs/components/block-preview.tsx`
- `apps/docs/app/docs/charts/chart-preview.tsx`

## Implementation

### Block Preview (`block-preview.tsx`)
1. The block preview uses `React.lazy()` + `Suspense` to dynamically import block components from `packages/blocks/*`
2. Blocks are rendered at 50% scale in a container — verify this still works with StlProvider
3. Check that the dynamic import paths resolve correctly (e.g., `../../packages/blocks/auth/AuthBlock`)
4. Verify default props are sufficient for meaningful visual rendering (blocks need mock data)
5. If blocks need a specific wrapper (like a form context or router), add minimal wrappers
6. The error boundary should gracefully handle any blocks that can't render

### Chart Preview (`chart-preview.tsx`)
1. Charts use `useEffect` + `import()` for dynamic loading — verify this pattern works with StlProvider
2. Check that chart components receive proper props:
   - `data` array with chart data points
   - `config` object with series definitions (label + color)
3. Chart colors currently use token references (`$blue9`, `$green9`, etc.) — these may not resolve without proper token context. If they don't resolve, replace with hex/hsl values or CSS variable references
4. Ensure all 6 chart types render (area, bar, line, pie, radar, radial)
5. The chart preview container needs proper dimensions (height/width) for charts to render

### Common fixes
- Both previews should inherit StlProvider from root — no additional provider wrapping needed
- Both use `'use client'` directive — correct for client-side rendering
- Both have loading/error states — verify they work

## Acceptance
- All 10 block pages show visually styled, rendered block previews (not just text/unstyled HTML)
- All 6 chart pages show rendered charts with colors and proper layout
- Block variant selector works (switching between block variants renders correctly)
- Error boundaries catch and display failures gracefully
- No console errors related to missing providers or context
- Commit with conventional commit message
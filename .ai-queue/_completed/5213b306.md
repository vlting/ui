<!-- LAT: 2026-03-10T22:36:31Z -->
<!-- PID: 96331 -->
<!-- auto-queue -->
<!-- target-branch: fix/a11y-audit/audit-fix -->
# Task: Fix blocks a11y + update Playwright a11y tests

## Scope
Two parts:

### Part 1: Fix ARIA/a11y for all blocks (`packages/blocks/`)
- app-shell, auth, chat-interface, dashboard, data-table, empty-state, feed, file-upload, hero, notification-center, onboarding-wizard, pricing, settings, sidebar

### Part 2: Update Playwright a11y tests (`apps/showcase-web/e2e/pages-a11y.spec.ts`)

## Instructions

### Part 1: Block accessibility

1. **Read each block's source files** (under `packages/blocks/src/{block}/`).

2. **Fix structural ARIA:**
   - app-shell: proper landmark regions (`<header>`, `<main>`, `<nav>`, `<aside>`)
   - sidebar: `<nav>` or `<aside>` with `aria-label`
   - auth: form labels, required fields, error messages with `aria-describedby`
   - dashboard: proper heading hierarchy (`h1` → `h2` → `h3`), card regions
   - data-table: proper `<table>` semantics, sortable `<th>` with `aria-sort`
   - chat-interface: `role="log"` or `aria-live="polite"` on message list, `aria-label` on input
   - feed: `role="feed"` with `aria-label`, articles with `role="article"`
   - file-upload: `aria-label` on drop zone, upload status announcements
   - hero: heading hierarchy, CTA button labels
   - notification-center: `aria-live="polite"` for new notifications
   - onboarding-wizard: step indicator with `aria-current="step"`, progress communication
   - pricing: comparison table with proper headers, CTA labels
   - settings: form labels, fieldset/legend for groups
   - empty-state: proper heading, action button labels

3. **Ensure proper heading hierarchy** within each block (no skipped levels).

### Part 2: Playwright a11y tests

4. **Update `apps/showcase-web/e2e/pages-a11y.spec.ts`:**
   - Remove `button-name` from `KNOWN_ISSUES` (components should now have proper names)
   - Keep `color-contrast` and `target-size` in KNOWN_ISSUES (visual polish, not structural a11y)
   - Add a `Charts` page entry if `/charts` route exists
   - Add a `Utils` page entry if `/utils` route exists
   - Ensure the test covers all routes in the showcase app

5. **Add keyboard navigation smoke tests** in a new file `apps/showcase-web/e2e/keyboard-nav.spec.ts`:
   ```typescript
   // Test Tab navigation through interactive components on key pages
   // Test Enter/Space activation on buttons
   // Test Escape to close overlays (if any open on the page)
   // Keep tests lightweight — smoke tests, not exhaustive
   ```

## Do NOT
- Add color-contrast fixes
- Modify component or primitive source files (covered by other tasks)
- Add heavy test infrastructure — keep tests simple

## Acceptance
- All blocks use semantic HTML landmarks and proper heading hierarchy
- Playwright a11y tests cover all showcase pages
- `button-name` removed from known issues (fixed by other tasks)
- Keyboard nav smoke tests exist and are reasonable
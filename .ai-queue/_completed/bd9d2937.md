<!-- LAT: 2026-03-10T22:14:32Z -->
<!-- PID: 77812 -->
<!-- auto-queue -->
<!-- target-branch: feat/showcase-polish/api-coverage-polish -->
# Task: Expand showcase-native coverage with native primitives and components

## Context
The showcase-native app (`apps/showcase-native/`) currently has 5 basic screens (Home, Styling, Primitives, Components, Hooks). Need to expand coverage to include all available native primitives and components.

## Files to modify
- Files in `apps/showcase-native/` ONLY
- Screen files in `apps/showcase-native/app/` or `apps/showcase-native/src/`

## Implementation

1. **Check available native exports**: Read `packages/stl-native/src/primitives/` and `packages/components/index.native.ts` to understand what's available for React Native.

2. **Expand screen content**: Update existing screens to demo more components:
   - **Primitives screen**: Box, Stack, VStack, HStack, Text, Heading, Icon, Spacer, Badge, Spinner, Separator, etc.
   - **Components screen**: Button, Input, Switch, Checkbox, Card, Alert, Progress, etc.
   - **Hooks screen**: useControllableState, useDebounce, useMediaQuery, etc.

3. **Add missing screens** if there are component categories not yet covered (e.g., Forms, Data Display).

4. **Polish**: Ensure consistent spacing, clear section labels, good visual hierarchy.

5. **Verify**: Run `npx tsc --noEmit` in the showcase-native directory to verify TypeScript.

## Acceptance
- Showcase-native covers all available native primitives and components
- Screens have clear section labels and organized demos
- TypeScript compiles without errors
- Commit with conventional commit message
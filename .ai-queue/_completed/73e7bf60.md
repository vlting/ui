<!-- LAT: 2026-03-10T21:47:35Z -->
<!-- PID: 77812 -->
<!-- auto-queue -->
<!-- target-branch: feat/docs-polish/complete-docs-deploy -->
# Task: Create utilities documentation pages

## Context
The docs site has no utilities section. There are several utilities in `packages/utils/` that need documentation.

## Available utilities (in `packages/utils/`)
1. `cn` — className merger (clsx + tailwind-merge equivalent)
2. `mergeRefs` — merge multiple React refs into one
3. `composeEventHandlers` — compose multiple event handlers
4. `FontLoader` — component for loading Google Fonts
5. `getGoogleFontsUrl` — generate Google Fonts URL from config
6. `useFontLoader` — hook for font loading state
7. `isSystemFont` — check if a font family is a system font
8. `extractFamiliesFromConfig` — extract font families from brand config
9. `buildFaceMapsFromConfig` — build font face maps from brand config

## Files to create/modify
- `apps/docs/app/docs/utilities/page.tsx` — utilities index page (NEW)
- `apps/docs/app/docs/utilities/[name]/page.tsx` — individual utility page (NEW)
- `apps/docs/lib/utility-registry.ts` — utility registry (NEW)
- `apps/docs/lib/navigation.ts` — add utilities section (coordinate with Task 001 which also modifies this file — add BOTH hooks and utilities sections)

**IMPORTANT**: Task 001 also modifies `apps/docs/lib/navigation.ts`. If Task 001 has already run, read the updated file before modifying. Add the utilities section alongside the hooks section. If Task 001 hasn't run yet, add both hooks AND utilities sections yourself.

## Implementation

1. **Create `apps/docs/lib/utility-registry.ts`**: Registry with each utility. Read source files to extract signatures. Group into:
   - "Styling": cn
   - "React Helpers": mergeRefs, composeEventHandlers
   - "Font Loading": FontLoader, getGoogleFontsUrl, useFontLoader, isSystemFont, extractFamiliesFromConfig, buildFaceMapsFromConfig

2. **Create `apps/docs/app/docs/utilities/page.tsx`**: Index page listing all utilities grouped by category.

3. **Create `apps/docs/app/docs/utilities/[name]/page.tsx`**: Dynamic page for individual utilities. Show:
   - Name and description
   - Import statement
   - Usage example
   - Parameters / return value
   - Use `generateStaticParams()`

4. **Update navigation**: Add "Utilities" section.

## Acceptance
- All utilities have documentation pages
- Each page shows: import, usage example, parameter docs
- Utilities section in navigation
- `yarn build:docs` succeeds
- Commit with conventional commit message
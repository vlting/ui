# Commit History
- 16138a0 feat(component-parity): add DataTable barrel exports
- 4d80f91 Merge branch 'q/002' into feat/component-parity/data-table

<!-- auto-queue -->
<!-- depends-on: 001 -->
<!-- target-branch: feat/component-parity/data-table -->

# Barrel Export Updates for DataTable

Integration segment: update barrel exports to include DataTable and its type helpers.

## Scope

- `packages/components/index.ts` (modify)
- `src/index.ts` (modify)

## Context

- **Depends on:** Segment 001 (DataTable component) must be merged first.
- **Feature flag:** `component_parity`
- Read `AI_CONSTITUTION.md` before implementing.

## Instructions

### 1. `packages/components/index.ts`

Add DataTable exports alongside the existing component exports (near the Table export):

```typescript
export { DataTable } from './DataTable'
export type { DataTableProps } from './DataTable'
export type {
  ColumnDef,
  SortingState,
  ColumnFiltersState,
  VisibilityState,
  PaginationState,
} from './DataTable'
```

### 2. `src/index.ts`

Add DataTable to the Layer 2 component exports section:

```typescript
export {
  DataTable,
} from '../packages/components'
```

Add DataTable types to the type exports section:

```typescript
export type {
  DataTableProps,
  ColumnDef,
  SortingState,
  ColumnFiltersState,
  VisibilityState,
  PaginationState,
} from '../packages/components'
```

**Note:** Add `DataTable` to the existing value export block and the types to the existing type export block — don't create separate export statements.

## Verification

- `npx tsc --noEmit` — no type errors
- All existing tests still pass: `npx jest --passWithNoTests`

## Task Progress
<!-- lat: 2026-03-02T02:05:14Z -->
<!-- agent-pid: 36295 -->
<!-- worktree: .worktrees/q-002 -->
<!-- branch: q/002 -->

### Checklist
- [ ] **ACTIVE** → Add DataTable exports to packages/components/index.ts
- [ ] Add DataTable exports to src/index.ts
- [ ] Verify TypeScript compiles
- [ ] Verify tests pass

### Handoff Context
- Integration segment for DataTable barrel exports

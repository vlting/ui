<!-- LAT: 2026-03-10T03:51:53Z -->
<!-- PID: 28592 -->
<!-- auto-queue -->
<!-- target-branch: feat/theming-hardening/token-audit -->
# Task: Audit all STL token scales for completeness

## Goal
Audit all 22 scale files in `packages/stl/src/config/scales/` and cross-reference with `packages/design-tokens/base.ts` to identify gaps, inconsistencies, or missing tokens.

## Instructions

1. Read every scale file in `packages/stl/src/config/scales/` (animation, border, color, column, font, fontFamily, fontSize, fontWeight, lineHeight, outline, radius, row, shadow, size, space, textDecoration, typoSpace, typo, zIndex)

2. Read `packages/design-tokens/base.ts` to see what base values exist

3. For each scale, verify:
   - All expected steps are present (e.g., size 0-16 + semantic, space 0-16 + negatives, radius 0-12)
   - Light/dark mode correctness for color-dependent scales (color, shadow)
   - No orphaned or unused tokens
   - CSS variable naming is consistent

4. Create a report file at `packages/stl/SCALE_AUDIT.md` documenting:
   - Each scale name, number of tokens, any gaps found
   - Light/dark mode status (for applicable scales)
   - Recommended fixes (if any)

5. Fix any simple gaps found (missing tokens that should exist based on the pattern). For color gaps, only document them — do NOT modify colorGen.utils.ts (that's task 002).

## Key files
- `packages/stl/src/config/scales/*.ts` (all scale files)
- `packages/design-tokens/base.ts`
- `packages/stl/src/config/styles.css.ts` (where scales are composed)

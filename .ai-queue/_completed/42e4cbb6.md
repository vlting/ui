<!-- LAT: 2026-03-11T03:08:35Z -->
<!-- PID: 77812 -->
<!-- auto-queue -->
<!-- target-branch: feat/theme-overhaul/theme-system-core -->
# Task: Fix hightContrast typo and update conditions system

## Context
The conditions system in STL has a typo: `hightContrast` should be `highContrast`. This affects the conditions map, the condition categories, and the `mapConditions()` function.

## Requirements

### 1. Fix typo in `packages/stl/src/config/conditions.ts`

Find and replace ALL occurrences of `hightContrast` with `highContrast`:

- Line 37: `hightContrast` in `queryConditionsMap` → `highContrast`
- Line 84: `'!hightContrast'` in `conditionsMap` → `'!highContrast'`
- Line 113: `'!hightContrast'` in `mapConditions()` → `'!highContrast'`
- Line 149: `hightContrast` in `ConditionCategories` → `highContrast`
- Line 152: `'!hightContrast'` in `ConditionCategories` → `'!highContrast'`

### 2. Update any type definitions that reference `hightContrast`

Search `packages/stl/` for any TypeScript types, interfaces, or string literal unions that include `hightContrast`. These need to be updated to `highContrast` as well.

Check these files specifically:
- `packages/stl/src/config/styles.models.ts` — may have condition key types
- `packages/stl/src/config/styles.css.ts` — may reference condition keys
- `packages/stl/src/config/index.ts` — may export condition types

### 3. Update any references in stl-react

Search `packages/stl-react/` for `hightContrast` references. The `useContextConditions` hook or similar may reference it.

### 4. Update any references in stl-native

Search `packages/stl-native/` for `hightContrast` references.

## Files to modify
- `packages/stl/src/config/conditions.ts` (primary — fix typo)
- Any other files in `packages/stl/`, `packages/stl-react/`, `packages/stl-native/` that reference `hightContrast`

## Acceptance criteria
- Zero occurrences of `hightContrast` in the entire codebase
- All occurrences replaced with `highContrast`
- TypeScript compiles without errors
- No behavioral change — just a rename
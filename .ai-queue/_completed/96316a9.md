# Commit History
- 9c7c0e5 fix(components): replace hardcoded sizes with design tokens in overlay components
- 96316a9 Merge branch 'q/004' — token discipline for overlay components

<!-- target-branch: fix/quality-audit -->

# Segment 4: Token Discipline — Overlay Components

## Summary
- Drawer: HandleBar width/height/margins to size/space tokens, Title/Description fontSize to font tokens
- Popover: animation offset y to negative space token ($-0.5); offset prop kept as literal (Floating UI numeric)
- HoverCard: side margins moved from plain style object to Tamagui props with space tokens ($0.5)
- Dialog: no changes needed — all spacing already tokenized
- Tooltip: no changes needed — sideOffset is Floating UI numeric, arrow size is literal
- Toast: no changes needed — zIndex excluded per rules

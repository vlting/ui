<!-- LAT: 2026-03-10T22:47:13Z -->
<!-- PID: 96331 -->
<!-- auto-queue -->
<!-- target-branch: docs/readme-ship/final-verification -->
# Task: Fix lint, typecheck, and test issues for CI

## Context
The 3 GH workflows (lint, typecheck, test) have been failing on main (pre-existing). This task fixes the issues so all workflows pass.

## Pre-existing issues (from CI logs)
1. **Lint (biome):** Import sorting violations
2. **Typecheck:** STL package errors (`replaceAll` not found on string type, `ReactNode` type mismatches)
3. **Tests (jest):** Various test failures

## Instructions

### 1. Fix lint issues
Run `yarn lint` and fix all biome violations. Common fixes:
- Import sorting: biome wants grouped imports (external → internal → relative)
- Run `npx @biomejs/biome check --write .` to auto-fix where possible
- Only fix files that biome reports — don't reorganize non-violated files

### 2. Fix typecheck issues
Run `yarn typecheck` (or `npx tsc --noEmit`) and fix errors:
- `replaceAll` error in STL: add `"lib": ["ES2021"]` to the relevant tsconfig, or change to `.replace(/pattern/g, replacement)`
- `ReactNode` type mismatches: ensure consistent React types (likely `@types/react` version mismatch — check and align)
- Fix any other TypeScript errors reported

### 3. Fix test issues
Run `yarn test` and fix failing tests:
- Update outdated snapshots if applicable
- Fix broken assertions due to component changes from a11y epic
- Don't add new tests — just fix existing ones

### 4. Verify docs build
Run `cd apps/docs && yarn build` (or equivalent) and fix any build errors.

## Do NOT
- Add new features or components
- Refactor working code
- Modify CI workflow files
- Touch README.md (other task handles it)

## Acceptance
- `yarn lint` passes (exit 0)
- `yarn typecheck` passes (exit 0)
- `yarn test` passes (exit 0)
- `yarn build` succeeds at root level

<!-- LAT: 2026-03-09T14:37:50Z -->
<!-- PID: 5792 -->
<!-- auto-queue -->
<!-- target-branch: feat/stl-primitives/layout-primitives -->
# Task: Migrate packages/primitives/ to use stl-react

Replace Tamagui implementations in `packages/primitives/` with stl-react primitives.

## Context
- stl-react already has: Box, Column (→ VStack), Row (→ HStack)
- stl-react will have (from parallel task): Spacer, Divider, Separator, AspectRatio
- The `packages/primitives/` files are the public API surface consumed by other parts of the codebase
- Import stl-react primitives via relative path: `../../packages/stl-react/src/primitives/...` or the index `../../packages/stl-react/src`

## Files to modify

### `packages/primitives/Box.tsx`
Replace entire contents:
```tsx
// @ts-expect-error stl-react styled components need explicit type for re-export
export { Box } from '../stl-react/src/primitives/Box/Box'
export type { HTMLAttributes as BoxProps } from 'react'
```
Actually, simplify — just re-export and let consumers use the stl-react types:
```tsx
export { Box } from '../stl-react/src/primitives/Box/Box'
```
**Note:** The stl-react Box currently has no `centered` variant. Add the variant directly to the re-export if needed, or check if Box already got updated. If not, add a wrapper:
```tsx
import { styled } from '../stl-react/src/config'

export const Box = styled(
  "div",
  {},
  {
    centered: {
      true: { display: "flex", alignItems: "center", justifyContent: "center" },
    },
  },
  "Box"
)
```

### `packages/primitives/Stack.tsx`
Replace entire contents with re-exports from stl-react:
```tsx
import { Column } from '../stl-react/src/primitives/Column/Column'
import { Row } from '../stl-react/src/primitives/Row/Row'

export const VStack = Column
export const HStack = Row
export const Stack = Column
```

### `packages/primitives/Spacer.tsx`
Replace entire contents:
```tsx
export { Spacer } from '../stl-react/src/primitives/Spacer/Spacer'
```

### `packages/primitives/Divider.tsx`
Replace entire contents:
```tsx
export { Divider } from '../stl-react/src/primitives/Divider/Divider'
```

### `packages/primitives/Separator.tsx`
Replace entire contents:
```tsx
export { Separator } from '../stl-react/src/primitives/Separator/Separator'
export type { SeparatorProps } from '../stl-react/src/primitives/Separator/Separator'
```

### `packages/primitives/AspectRatio.tsx`
Replace entire contents:
```tsx
export { AspectRatio } from '../stl-react/src/primitives/AspectRatio/AspectRatio'
export type { AspectRatioProps } from '../stl-react/src/primitives/AspectRatio/AspectRatio'
```

## Acceptance criteria
- All 7 files updated (Box, Stack, Spacer, Divider, Separator, AspectRatio)
- Zero imports from `tamagui`, `@tamagui/*`
- Zero `@ts-expect-error` comments
- All files are simple re-exports from stl-react (thin wrappers at most)
- Stack/VStack/HStack aliases preserved for API compatibility

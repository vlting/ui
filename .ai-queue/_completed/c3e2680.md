<\!-- LAT: 2026-03-09T15:08:51Z -->
<\!-- PID: 5917 -->
<!-- auto-queue -->
<!-- target-branch: feat/stl-components/simple-components -->
# Task: Migrate form components (InputGroup, Form, Textarea, Input, Field, Checkbox, RadioGroup)

These components have form-specific logic (context, useId, etc.) with styled frames.

## Migration pattern
Import `styled` from `'../stl-react/src/config'`. Replace Tamagui styled() calls with stl equivalents. Keep all behavioral logic (context, hooks, state).

For components that wrap Tamagui form primitives (Input, Checkbox, RadioGroup), replace with native HTML + stl styling:
- `Input from @tamagui/input` → `styled("input", css)`
- `Checkbox from @tamagui/checkbox` → native `<input type="checkbox">` with stl styling
- `RadioGroup from @tamagui/radio-group` → native `<fieldset>` + `<input type="radio">` with stl styling

## Files to modify

### `packages/components/InputGroup/InputGroup.tsx`
- Replace `styled()` calls with stl equivalents
- Keep InputGroupContext for size propagation
- Keep dangerouslySetInnerHTML for border-radius CSS
- Remove @ts-expect-error comments (6)

### `packages/components/Form/Form.tsx`
- Replace `styled(YStack)`, `styled(Text)` with stl equivalents
- Replace Tamagui Form with native `<form>` element
- Keep FormFieldContext
- Remove @ts-expect-error comments (5)

### `packages/components/Textarea/Textarea.tsx`
- Replace `styled(TamaguiTextArea)` with `styled("textarea", css)`
- Replace `styled(YStack)`, `styled(Text)` with stl equivalents
- Keep useId for label association
- Remove @ts-expect-error comments (6) and `as any` casts

### `packages/components/Input/Input.tsx`
- Replace `Input from @tamagui/input` with `styled("input", css)`
- Keep useId for label/helper/error IDs
- Remove @ts-expect-error comments (7) and `as any` casts

### `packages/components/Field/Field.tsx`
- May have no Tamagui imports — verify and clean up
- Keep FieldContext and cloneElement logic

### `packages/components/Checkbox/Checkbox.tsx`
- Replace `Checkbox from @tamagui/checkbox` with native `<input type="checkbox">` wrapped in stl styling
- Keep check/indeterminate state handling

### `packages/components/RadioGroup/RadioGroup.tsx`
- Replace `RadioGroup from @tamagui/radio-group` with native `<fieldset>` + `<input type="radio">`
- Keep RadioGroupSizeContext

## Acceptance criteria
- Zero Tamagui imports in all 7 files
- Zero @ts-expect-error comments
- Form semantics preserved (labels, IDs, ARIA)
- All context patterns maintained
<!-- LAT: 2026-03-12T03:16:01Z -->
<!-- PID: 43768 -->
<!-- worktree: .worktrees/q-002 -->
<!-- branch: q-002 -->
<!-- auto-queue -->
<!-- target-branch: chore/foundation-reset/design-tokens-migration -->
<!-- depends-on: 001 -->
# Task: Update root exports and tsconfig paths

## What to do

1. In `package.json`:
   - Remove the `"./design-tokens"` export entry entirely
   - Keep `"./stl"` pointing at `./packages/stl/src/index.ts` (theme is now part of stl)

2. In `tsconfig.json`:
   - Remove the `"@vlting/ui/design-tokens"` path entry
   - Verify `"@vlting/stl"` path still points at `./packages/stl/src`

3. In `src/index.ts`:
   - Change all imports from `../packages/design-tokens` and `../packages/design-tokens/brands` to `../packages/stl/src/theme`
   - Rename re-exported types: `Brand` → `Theme`
   - Rename re-exported values: `defaultBrand` → `defaultTheme`, `funBrand` → `funTheme`, `poshBrand` → `poshTheme`, `shadcnBrand` → `shadcnTheme`
   - Remove deprecated re-exports: `injectBrandVars`, `getBrandStyleTag`
   - Keep `themeToVars`, `getThemeStyleTag`, `generateTheme`, `generatePalette`, `getTextColorStep`, all presets, all base tokens

## Files touched
- package.json
- tsconfig.json
- src/index.ts

## Acceptance criteria
- No references to design-tokens in package.json exports or tsconfig paths
- src/index.ts imports from stl/src/theme
- All Brand→Theme renames reflected in re-exports

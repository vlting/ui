<!-- LAT: 2026-03-10T05:07:01Z -->
<!-- PID: 28592 -->
<!-- auto-queue -->
<!-- target-branch: fix/docs-overhaul/fix-rendering -->
# Task: Fix docs app component rendering and empty previews

## Goal
Debug and fix the NextJS docs app so every component page shows a live preview.

## Instructions

1. Read the docs app structure:
   - `apps/docs/` — NextJS app
   - `apps/docs/app/` — App router pages
   - `apps/docs/lib/registry.ts` — Component registry
   - `apps/docs/components/` — UI components for docs

2. Try to build the docs app:
   ```bash
   cd apps/docs && npm run build 2>&1
   ```
   If it fails, capture the errors.

3. Identify the root cause of empty previews:
   - Is it SSR/hydration with STL/vanilla-extract?
   - Is it missing component imports?
   - Is the registry mapping wrong?

4. Common fixes:
   - Add `"use client"` directives where needed
   - Fix import paths for STL components
   - Ensure vanilla-extract CSS is loaded
   - Fix Next.js config for external packages

5. Fix the issues and verify:
   - Every component page shows a live preview
   - Both light and dark mode work
   - No hydration mismatches in console

6. If the docs app is too broken to fix in one task, at minimum:
   - Get it building without errors
   - Fix the top 5 most important component pages (Button, Input, Card, Dialog, Tabs)
   - Document remaining issues

## Key files
- `apps/docs/` (entire docs app)
- `apps/docs/next.config.mjs`
- `apps/docs/lib/registry.ts`

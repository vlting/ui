<!-- auto-queue -->
# Shadow Audit and Hardcoded Value Purge for Remaining Components

Comprehensive sweep of ALL remaining component files to replace hardcoded styling values (especially shadows and colors) with theme tokens. The user's core requirement: **virtually all styling values in the UI lib must come from the brand config/theme, not from hardcoded values.**

## Components to Audit

### Known hardcoded shadows:

1. **DatePicker** (`packages/components/DatePicker/DatePicker.tsx`)
   - Line ~152: `boxShadow: '0 4px 12px rgba(0,0,0,0.1)'`
   - Replace with theme shadow token

2. **DateRangePicker** (`packages/components/DatePicker/DateRangePicker.tsx`)
   - Line ~152: `boxShadow: '0 4px 12px rgba(0,0,0,0.1)'`
   - Replace with theme shadow token

3. **InputOTP** (`packages/components/InputOTP/InputOTP.tsx`)
   - Line ~148: `boxShadow: '0 0 0 2px var(--ring, rgba(0,0,0,0.1))'`
   - Replace with theme token for focus ring (use `$outlineColor` or `$color5`)

### Drawer verification:

4. **Drawer** (`packages/components/Drawer/Drawer.tsx`)
   - Current code uses theme tokens (`$background`, `$borderColor`, `$overlayBackground`, `$color6`, etc.) — appears correct
   - But user reports white shadows on drawers/panels. Investigate:
     - Does Tamagui's Dialog.Content apply a default shadow using `$shadowColor`? (If so, the theme template bug `shadowColor: 0` = white would cause white shadows even though Drawer code doesn't explicitly set shadows)
     - Check if Dialog.Content from `@tamagui/dialog` applies default `shadowColor` or `boxShadow` styles
     - If yes, explicitly set dark shadow values on the Drawer's `<DialogContent>`: `shadowColor="$shadowLgColor"` or `style={{ boxShadow: 'var(--shadowLg)' }}`
   - Also check: borderRadius values (currently hardcoded `10`) — use `$5` or `$6` token instead

### Comprehensive grep for remaining hardcoded values:

5. Run a thorough search across ALL files in `packages/components/` for:
   - Hardcoded `boxShadow` strings
   - Hardcoded hex colors (`#fff`, `#000`, `#` followed by hex digits)
   - Hardcoded `rgb(` or `rgba(` values in style props
   - Hardcoded `borderRadius` pixel values that should be tokens

   Fix every instance found. Components in segments 001 and 002 are already being handled — skip those files. Focus on ALL OTHER component files.

### Components NOT covered by segments 001/002 (check these):
- `packages/components/Tooltip/Tooltip.tsx`
- `packages/components/Card/Card.tsx`
- `packages/components/Alert/Alert.tsx`
- `packages/components/Avatar/Avatar.tsx`
- `packages/components/Breadcrumb/Breadcrumb.tsx`
- `packages/components/Button/Button.tsx`
- `packages/components/ButtonGroup/ButtonGroup.tsx`
- `packages/components/Calendar/Calendar.tsx`
- `packages/components/Carousel/Carousel.tsx`
- `packages/components/Checkbox/Checkbox.tsx`
- `packages/components/Collapsible/Collapsible.tsx`
- `packages/components/Combobox/Combobox.tsx`
- `packages/components/Command/Command.tsx`
- `packages/components/Form/Form.tsx`
- `packages/components/Input/Input.tsx`
- `packages/components/Loader/Loader.tsx`
- `packages/components/NativeSelect/NativeSelect.tsx`
- `packages/components/Progress/Progress.tsx`
- `packages/components/RadioGroup/RadioGroup.tsx`
- `packages/components/Select/Select.tsx`
- `packages/components/Slider/Slider.tsx`
- `packages/components/Switch/Switch.tsx`
- `packages/components/Table/Table.tsx`
- `packages/components/Tabs/Tabs.tsx`
- `packages/components/Textarea/Textarea.tsx`
- `packages/components/Toggle/Toggle.tsx`
- `packages/components/Typography/Typography.tsx`
- `packages/components/Accordion/Accordion.tsx`
- `packages/components/Pagination/Pagination.tsx`
- Any other `.tsx` files in `packages/components/`

Also check:
- `packages/primitives/*.tsx` — all primitive components
- `packages/hooks/*.ts` — any hardcoded values in hooks

## Scope
- `packages/components/DatePicker/DatePicker.tsx`
- `packages/components/DatePicker/DateRangePicker.tsx`
- `packages/components/InputOTP/InputOTP.tsx`
- `packages/components/Drawer/Drawer.tsx`
- `packages/components/Tooltip/Tooltip.tsx`
- Any other component files found to have hardcoded shadow/color values (excluding files owned by segments 001 and 002)

## Acceptance Criteria
- No hardcoded `boxShadow`, `rgb(...)`, `rgba(...)`, or hex color values in any component file (except where truly necessary, e.g., CSS variable fallbacks)
- All shadows use `$shadowSm`, `$shadowMd`, `$shadowLg` (or their Color variants) from the theme
- Drawer/panels do not have white shadows — shadows are dark and come from theme
- `borderRadius` uses token values where possible
- TypeScript compiles cleanly
